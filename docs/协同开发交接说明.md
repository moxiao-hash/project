# StudyPilot 协同开发交接说明

本文档是 Codex 与 Gemini 交替开发时的统一交接入口。任何开发者开始工作前，应先阅读本文档、对应实施计划和最近提交；结束一个小步骤时，应同步更新本文档，避免只在对话中留下关键信息。

## 固定交接规则

每个 Task 或可独立验证的小步骤都必须记录：

1. 当前状态：`未开始 / 开发中 / 已完成 / 被阻塞`。
2. 已实现范围及明确未实现范围。
3. 关键代码入口和重要设计取舍。
4. 新增或修改的数据库、接口、配置及安全边界。
5. 已执行的测试命令、测试结果和已知限制。
6. 对应 Git 提交号，以及未提交文件的归属和原因。
7. 下一位开发者可以直接执行的下一步。

生产代码继续遵循：先写失败测试、实现最小代码、局部验证、全量验证、更新文档、独立提交并推送。

## 当前工作基线

- 分支：`main`
- 最近完成：Task 26，全栈真实联调、文档与演示验收
- 当前开发：无；Task 26 已完成
- 后续任务：整理生产环境部署配置与未来付费/进阶学习闭环演进
- 总计划：[`docs/superpowers/plans/2026-09-04-agent-native-studypilot.md`](superpowers/plans/2026-09-04-agent-native-studypilot.md)
- 协同变更：开发期间保留所有交接规范与全量测试套件绿灯基线。

## Task 26 工作记录

状态：已完成，预定功能提交 `test: complete agent native studypilot workflow`。

### 1. 当前状态
`已完成`。

### 2. 已实现范围及明确未实现范围
- **已实现范围**：
  1. 真实全栈环境与服务健康探针验证（MySQL 9.6 端口 3306、Spring Boot 端口 8080、FastAPI 端口 8000、Vue 端口 5173、Docker 容器 Runner 与 Unix Socket）。
  2. 统一 Agent 原生完整闭环集成测试 `AgentNativeWorkflowE2ETest.java`，涵盖：身份防伪造注入、AI 凭据脱敏防护、动态上下文注入、受治理动作卡（`WAITING_CONFIRMATION`）、独立专用确认卡、SSE 递增 sequence 与 `Last-Event-ID` 断线续传重放、Local Runner 白名单只读测试预览（`MAVEN_TEST`）、个人运行健康指标（`/api/assistant/health`）。
  3. 全链路 HTTP 验收脚本与结果文档：交付 `docs/agent-native-e2e.http` 与 `docs/agent-native-e2e-result.md`。
  4. 更新 `docs/部署与演示指南.md`，补充 Agent 原生全链路五分钟演示路线与环境约束。
  5. 修复 `runner-service` 3 处 import ordering ruff 告警。
- **明确未实现范围**：
  1. 不支持外部模型宕机时的无缝自动平替（目前降级为本地已有数据与传统界面操作）。
  2. 不开放任意 Shell 命令执行或任意 URL/键鼠控制。

### 3. 关键代码入口和重要设计取舍
- `backend/src/test/java/com/moxiao/studypilot/agent/api/AgentNativeWorkflowE2ETest.java`：端到端模拟完整统一会话、SSE 流式事件与专用确认卡流转，杜绝客户端通过请求体伪造 `ownerId`。
- `docs/agent-native-e2e.http`：提供涵盖运维探针、认证注册、凭据保护、统一会话、流式续传、专用确认与 Runner 预览的完整操作脚本。
- `docs/agent-native-e2e-result.md`：详细记录端到端验收结果与四层信任边界核查记录。

### 4. 新增或修改的数据库、接口、配置及安全边界
- **接口与安全边界**：
  - 前端仅调用 Java `/api/**`，内部 `internalToken` 与凭据绝不泄露给浏览器。
  - 高风险写操作与命令执行均必须生成 `WAITING_CONFIRMATION` 预览卡，聊天文本无法替代专用确认接口。
  - Runner 执行依赖短期 HMAC-SHA256 签名信封，容器采用断网 `--network none`、只读根与 tmpfs 隔离。

### 5. 已执行的测试命令、测试结果和已知限制
- **测试命令与结果**：
  - Java：`cd backend && mvn test -q`（362 项全部通过，新增 1 项 E2E 综合测试）。
  - AI 服务：`ai-service/.venv/bin/python -m pytest -q ai-service/tests && ai-service/.venv/bin/ruff check ai-service`（302 项通过，Ruff 通过）。
  - Runner 服务：`PYTHONPATH=runner-service ai-service/.venv/bin/python -m pytest -q runner-service/tests && ai-service/.venv/bin/ruff check runner-service`（21 项通过，Ruff 通过）。
  - 前端：`cd web && npm test -- --run && npm run build`（124 项测试通过，vue-tsc 与生产构建通过）。
  - 代码一致性：`git diff --check`（通过）。
- **已知限制**：
  - 本地 SQLite 与本地 Qdrant 保持单 FastAPI 进程部署；多进程会被文件锁拒绝。
  - Runner 执行依赖宿主机安装 Docker 或 Podman，无容器引擎时拒绝执行。

### 6. 对应 Git 提交号，以及未提交文件的归属和原因
- 本次变更即将提交为 `test: complete agent native studypilot workflow`。
- 涉及新增与修改文件均属于 Task 26 全链路验收测试、文档与演示指南。

### 7. 下一位开发者可以直接执行的下一步
- 执行日常演示或联调时，可按照 `docs/部署与演示指南.md` 章节 5.2 运行端到端 5 分钟演示路线。
- 参考 `docs/future-learning-agent-direction.md` 推进后续进阶付费体系与高级学习 Agent 功能演进。

## Task 25 工作记录

状态：已完成并推送，功能提交 `193e26e`。

计划分为三个小步骤：

1. 根据工作区技术栈和变更文件，确定性推荐 Maven、npm、pytest 等白名单测试，并继续通过隔离 Runner 执行。
2. Git commit 与 Git push 建立两个互相独立的高风险动作；两者分别预览、分别确认，普通聊天文本不能代替确认接口。
3. 建立 API 优先的界面操作策略；只有能力目录确认没有业务 API 时，才允许调用预注册的 Playwright DOM 或 IDEA 无障碍动作，禁止任意 URL、选择器、脚本和键鼠操作。

### 已完成

- `WorkspaceDeveloperService`：增加测试推荐、commit/push 预览、摘要冲突保护、指定文件 commit 与 origin push。
- `AgentReadToolConfiguration` / `AgentWriteToolConfiguration`：发布推荐、预览、commit 与 push 类型化工具。
- `UnifiedAgentSupervisor`：支持“运行修改后的测试”“提交刚才修改”“推送刚才的提交”，多个工作区或多个改动文件时会澄清而不是猜测。
- `InterfaceFallbackPolicy`：业务 API 优先，只允许固定 Playwright/IDE 动作且拒绝任意操作目标。
- 前端与对接文档已同步 `GIT_COMMIT`、`GIT_PUSH` 执行类型和独立确认规则。

### 验证结果

- Java：361 项通过。
- AI 服务：302 项通过，Ruff 通过。
- Runner：21 项通过，Ruff 通过。
- 前端：124 项通过，TypeScript 与生产构建通过。
- `git diff --check`：通过。

### 已知边界

- 自动测试一次只提交推荐列表中的第一种模板，保证单轮最多一个写操作；其他技术栈需新开一轮执行。
- 界面兜底目前提供策略与白名单适配边界，不开放任意电脑控制。实际 Playwright/IDE 适配器必须遵循该注册表，并在 Task 26 真实环境验收。
- SSH push 依赖本机已有的合法 Git/SSH 配置；凭据不会进入模型、FastAPI 或 Runner 容器。

### 下一步

- 执行 Task 26 全栈真实联调；若由 Gemini 接手，先运行各服务健康检查，再按总计划的关键验收场景逐条验证。
- Task 26 完成后，将本节提交信息补充为最终提交号，并记录演示步骤与环境限制。
