# StudyPilot 协同开发交接说明

本文档是多个 Agent（Codex、ZCode、DeepSeek Harness 等）交替开发时的统一交接入口。任何开发者开始工作前，应先阅读本文档、对应实施计划和最近提交；结束一个小步骤时，应同步更新本文档，避免只在对话中留下关键信息。

## 固定交接规则

每个 Task 或可独立验证的小步骤都必须记录：

1. 当前状态：`未开始 / 开发中 / 已完成 / 被阻塞`。
2. 已实现范围及明确未实现范围。
3. 关键代码入口和重要设计取舍。
4. 新增或修改的数据库、接口、配置及安全边界。
5. 已执行的测试命令、测试结果和已知限制。
6. 对应 Git 提交号，以及未提交文件的归属和原因。
7. 下一位开发者可以直接执行的下一步。

生产代码继续遵循：先写失败测试、实现最小代码、局部验证、全量验证、更新文档、独立提交并推送。

## 三 Agent 正式分工（2026-09-08 起）

本节是后续 Task 27～34 的权威协作规则。模型能力、Harness 名称和实际模型映射是三个不同概念；每次交接必须记录服务实际返回的 model ID，不能只依据界面标签判断。

| 角色 | 运行端 | 主责 | 不得单独决定 |
|---|---|---|---|
| 总架构师与验收负责人 | Codex | 全局设计、任务拆分、契约冻结、安全边界、代码审查、真实复跑、合并 `main`、完成状态 | 不以执行 Agent 的自述代替证据；不跳过测试直接批准 |
| 后端与 Agent 执行工程师 | DeepSeek Harness + DeepSeek V4 Flash | Python/FastAPI/LangGraph、Java Tool Gateway、治理与幂等、模型用量、Runner/Git 后端、自动化测试 | 权限放宽、风险降级、删除迁移、密钥处理、全栈完成结论 |
| 前端与体验执行工程师 | ZCode + Gemini 3.8 Flash | Vue Agent 首页、SSE 消费、UI Action、Playwright、视觉/响应式/无障碍、跨目录接口核对 | Java/Python 安全策略、任意浏览器或桌面权限、全栈完成结论 |

### 任务分配

| Task | 主执行 | 配合 | Codex 验收重点 |
|---|---|---|---|
| 27 能力矩阵与契约 | Codex | DeepSeek Harness 盘点工具；ZCode 盘点页面 | 工具、页面、风险和真实性限制一一对应 |
| 28 模型多步 Planner | DeepSeek Harness | ZCode 做长对话反例 | 结构化输出不能绕过 Java 策略 |
| 29 持续 SSE | DeepSeek Harness 负责 Python/Java；ZCode 负责 Vue | 双方按冻结事件契约联调 | 先持久化、可续传、不重复副作用 |
| 30 全业务工具与 UI Action | DeepSeek Harness 负责工具；ZCode 负责动作 | 双方只通过契约衔接 | 用户答题、打卡和成果接受不可代办 |
| 31 用量与预算 | DeepSeek Harness 负责采集/存储；ZCode 负责健康页 | — | 官方价格版本、幂等计费、未知价格不记零 |
| 32 Developer Agent 加固 | DeepSeek Harness | ZCode 做跨文件与恢复审查 | 路径、密钥、补丁、commit/push 边界 |
| 33 本地界面适配 | ZCode 负责适配器；DeepSeek Harness 负责签名/Java Handler | — | 白名单、Unix Socket、最小权限、真实回执 |
| 34 真实全栈验收 | DeepSeek Harness 负责环境证据；ZCode 负责浏览器证据 | — | Codex 独立复跑后才允许关闭 Task 20/25/26 |

### 分支、文件和交接协议

1. Codex 维护 `main`；两个执行 Agent 使用独立 worktree，不直接在 `main` 开发。
2. DeepSeek Harness 分支命名 `agent/deepseek-task-<编号>-<短名>`；ZCode 分支命名 `agent/zcode-task-<编号>-<短名>`。
3. 同一时刻同一文件只能有一个 Owner。跨端并行前先由 Codex 冻结 DTO、事件和错误码。
4. 执行 Agent 的交付状态只能写为“待 Codex 验收”，并提供 RED/GREEN 命令、结果、改动文件、未覆盖项和提交号。
5. 详细证据放入 `docs/verification/task-<编号>.md`；本文只保留当前状态和下一步，不粘贴大段日志。
6. Codex 按“规格 → 安全 → diff → 局部测试 → 全量测试 → 真实联调 → 文档 → 合并”验收。
7. Mock、H2、Stub、策略测试必须明确标记；只有真实服务、数据库/API、页面或 Git 远端回查才能标记 `REAL_E2E`。
8. 合并冲突由 Codex 处理；任何 Agent 都不得强推、硬重置或覆盖他人的工作树。

详细执行清单：[Task 27～34：Agent 产品化与三 Agent 协作计划](superpowers/plans/2026-09-08-agent-productization-and-multi-agent-collaboration.md)。

## 历史协作 Agent 登记（仅供溯源）

每个参与本项目开发的 Agent 首次接手时在此登记，便于其他 Agent 快速判断其上下文、工具边界、行为约定和交接入口。历史协作者 Codex 与 Gemini 的记录分散在下方各 Task 工作记录中。

| Agent | 登记时间 | 状态 | 说明 |
|---|---|---|---|
| Claude（旧 ZCode Harness 配置） | 2026-09-08 | 历史记录，已被上方正式分工取代 | 详见下方 |
| DeepSeek Harness（旧模型配置） | 2026-09-08 | 历史记录，运行时需重新核验 model ID | 详见下方 |
| Codex (GPT) | — | 历史协作者 | 见 Task 26、Task 25 等工作记录 |
| Gemini | — | 历史协作者 | 见 Task 25 等功能提交记录 |

### Claude（旧 ZCode Harness 配置）

**运行环境**

- 身份：Claude Opus / Sonnet (ZCode Harness 交互式 Agent)。
- 驱动模型：`claude-opus-4-6-thinking`。
- 工作目录：`/Users/moxiao/IdeaProjects/project`。
- 工具边界：完整终端 Shell（`Bash`）、安全文件编辑（`Read`/`Edit`/`Write`）、自动化测试运行（Maven/Pytest/Vitest）、Git 命令执行、Obsidian 本地知识库同步。
- 语言：与用户沟通、代码注释和文档一律使用简体中文。

**已读基线与环境理解**

- 熟悉全栈信任边界（Vue ➔ Spring Boot ➔ FastAPI ➔ Container Runner）、API 优先策略、高风险操作专用确认机制及七项固定交接规则。
- 已深入了解协作者生态：
  1. **GPT (Codex)**：历史架构规划者与关键功能实现者，固化了全栈开发基线与 7 项交接守则；
  2. **DeepSeek Harness (DSH)**：新加入成员（基于 `deepseek-v4.1-flash`，Web GUI `http://127.0.0.1:3080`），强调严谨真实证据层级、区分「测试套件通过」与「全栈端到端真实通过」；
  3. **Gemini**：多模态及未来方向规划协同者。

**行为约定**

1. 遵循 TDD 闭环：确定验收条件 → 失败测试 → 最小代码 → 局部验证 → 全量测试 → 更新文档 → 独立提交。
2. 保持真实性边界：不在无真实环境证据时虚假标记 PASS，不破坏持久化安全密钥配置。
3. 严格遵守知识库同步规范，重大任务完成后主动归档至 Obsidian 个人知识库。

### DeepSeek Harness（旧模型配置）

**运行环境**

- 身份：DeepSeek Harness 编码 Agent，通过 Web GUI `http://127.0.0.1:3080` 与用户交互。
- 模型：`deepseek-v4.1-flash-expires-on-0910`。
- 工作目录：`/Users/moxiao/IdeaProjects/project`。DSH 实现仓库位于 `/Users/moxiao/Desktop/Deepseek Harness/`，只用于扩展 DSH 本身，不参与本项目业务开发。
- 文件权限：`workspace-write`，只能修改工作区内文件；审批策略 `ask`，越权操作需用户批准，被拒绝后不绕道重试。
- 语言：与用户沟通、代码注释和文档一律使用简体中文。

**已读基线（接手时已确认）**

- `README.md`、本文档、`项目开发步骤.md`
- `docs/superpowers/plans/2026-09-04-agent-native-studypilot.md`（总计划）
- `docs/agent-native-e2e-result.md`（真实证据层级与未验收清单）
- `docs/architecture.md`

**行为约定**

1. 遵守统一开发周期：确定验收条件 → 写失败测试 → 实现最小代码 → 局部验证 → 全量验证 → 更新文档 → 独立提交并推送。
2. 每个小步骤结束后同步更新本文档和 `项目开发步骤.md`，按固定交接规则记录 7 项；不把关键信息只留在对话中。
3. 严格区分「自动化套件通过」与「真实端到端验收」：无实际运行证据不勾选完成，不把 H2/模拟上游结果写成全链路 PASS，不复活已被撤回的旧结论。
4. 不改动用户凭据、`.env` 和本地私有配置，不输出密钥、Token 或真实用户数据；演示账户只用于验证且不删除用户数据。
5. 所有写操作（commit、push、补丁、依赖准备、Rubric 接受）保持高风险分级与专用确认，普通聊天文本不能代替确认接口。

**交接入口**

- 当前 Task 状态：总计划 `docs/superpowers/plans/2026-09-04-agent-native-studypilot.md`。
- 真实证据层级与未验收清单：`docs/agent-native-e2e-result.md` 第 5、6 节。
- 全量验证命令：见 `README.md`「全量验证」；三个服务端安全值 `INTERNAL_SERVICE_TOKEN`、`AI_CREDENTIAL_MASTER_KEY`、`LANGGRAPH_AES_KEY` 必须保持稳定。

## 当前工作基线

- 当前验收分支：`agent/zcode-task-27-matrix-gate`（切自 `main`，基线提交 `08fa662`）。
- 当前任务：Task 27 已由 Codex 修正并验收通过；初始提交 `21f54b0`，修正提交 `b1c4c5a`，等待合入 `main`。
- 下一步骤：合并后进入 Wave 1；Task 28（多步模型 Planner）和 Task 29（持续 SSE）必须从修正后的契约分支开始，不得沿用 59 工具旧基线。
- 总计划：[Agent 原生化执行清单](superpowers/plans/2026-09-04-agent-native-studypilot.md)。
- 产品化总计划：[Agent 产品化与三 Agent 协作计划](superpowers/plans/2026-09-08-agent-productization-and-multi-agent-collaboration.md)。

## Task 27 工作记录（2026-09-09 Codex 验收通过）

状态：已完成并通过 Codex 门禁验收。初始提交 `21f54b0`，Codex 修正提交 `b1c4c5a`。

### 1. 当前状态
`已完成 / 已验收`，等待快进合入 `main`。

### 2. 已实现范围及明确未实现范围
- **已实现范围**：
  1. 门禁脚本 `scripts/verify-agent-capability-matrix.mjs`：从生产 Java 配置、Vue Router、Vue Dispatcher 和 Java Navigation Handler 提取事实，覆盖 Vue 31 个路由与 Java 64 个工具；不再从可能漏项的测试清单读取工具真相。
  2. 完整 v2 能力矩阵 `docs/agent-capability-matrix-v2.md`：覆盖全系统 31 个具名路由与 64 个已注册 Java 工具，逐项分类 `AUTO_READ / AUTO_NAVIGATE / PREVIEW_WRITE / USER_ONLY / UNSUPPORTED`。
  3. v2 核心契约冻结 `docs/agent-native-contract.md`：固定 `AssistantPlan`、`AssistantPlanStep`、`UiAction`、`PendingToolAction`、`AssistantEvent` 与 `Usage` 结构规范及 6 项硬性模型输入/输出安全禁令。
  4. 多 Agent 统一验证证据规范与模板 `docs/verification/README.md` 与实际归档证据 `docs/verification/task-27.md`。
- **明确未实现范围**：
  - 本任务没有实现 Planner 或 SSE；仅为修复文档声明与运行时不一致，补齐了 Java/Vue 确定性导航白名单。

### 3. 关键代码入口和重要设计取舍
- `scripts/verify-agent-capability-matrix.mjs`：作为后续 CI 与提交门禁，确保任何新页面或新工具必须同步更新能力矩阵，杜绝接口与能力漂移。
- `docs/agent-native-contract.md`：严格限制模型不得输出 URL、SQL、CSS 选择器、脚本或 ownerId，保证四层信任边界不可逾越。

### 4. 新增或修改的数据库、接口、配置及安全边界
- 冻结 `AssistantPlan` 最多 8 步、最多 1 个写操作、最多 1 次联网搜索的硬边界。
- 固化 28 个可导航页面的 `UiAction` 白名单 routeKey 与安全实体参数校验规则；其余 3 个路由为登录、注册和 404，不允许 Agent 导航。

### 5. 已执行的测试命令、测试结果和已知限制
- **门禁脚本测试**：
  - 缺失矩阵时运行 `node scripts/verify-agent-capability-matrix.mjs`，预期失败退出（退出码 1，RED 达成）。
  - Codex 修正后运行，输出 `[SUCCESS] 能力矩阵校验通过！覆盖全部 31 个页面路由与 64 个 Java 工具`；门禁自身 Node 测试 4/4 通过。
- **Java 全量测试**：`cd backend && ./mvnw test`（365 passed）。
- **前端全量测试**：`cd web && npm test -- --run`（124 passed）；`npm run typecheck` 与 `npm run build` 通过。
- **格式检查**：`git diff --check` 通过。详细 RED/GREEN 见 `docs/verification/task-27.md`。

### 6. 对应 Git 提交号，以及未提交文件的归属和原因
- 分支：`agent/zcode-task-27-matrix-gate`。
- 初始提交：`21f54b0`；Codex 修正：`b1c4c5a`；最终审批文档提交见本段后续 Git 历史。

### 7. 下一位开发者可以直接执行的下一步
- 将已验收分支快进合入 `main`。
- Wave 1 从最新 `main` 创建独立工作树：**Task 28（多步模型 Planner）** 与 **Task 29（持续 SSE）**；不得从旧提交 `21f54b0` 单独分支。

## Task 26 工作记录（2026-09-08 修正）

### 已修正

1. 将 `AgentNativeWorkflowE2ETest` 更名为 `AssistantFacadeContractTest`，明确 H2 + 模拟 Python 上游只验证门面。响应改为 `READY/pendingAction/uiActions/TODAY/params/reason`，补齐动作版本、执行 ID、过期时间。
2. 门面测试检查游标和 owner 注入、过滤已消费事件、顺序及确认转发；明确断言模拟 COMPLETED 不会修改真实 Java 任务。
3. `GovernedAgentToolWorkflowTest` 使用真实 Java 治理服务，断言确认后 COMPLETED、completedAt、version=2、任务历史仅一条、执行 SUCCEEDED、审计创建/确认/状态更新；重复确认不新增审计。
4. 重写 `docs/agent-native-e2e.http` 为确定性的学习时长调整流程；新增 `scripts/agent-native-smoke.py` 自动检查真实公共 API 的业务结果和事件集合。不登记真实源码目录、不执行 Git、不输出凭据。
5. 真实联调发现新用户没有路线时 `AgentLearningContextService` 捕获内层事务异常后，外层仍被标记 rollback-only，导致消息 503。先用新用户集成测试复现 `UnexpectedRollbackException`，再新增 `RoadmapQueryService.currentMapIfPresent`，用 Optional 表示未加入路线。原公共路线接口的 404 语义不变，不全局放宽回滚策略。
6. 修正总计划、项目步骤、演示指南和验收报告的完成口径。原 2026-09-07 全栈 PASS 声明已撤回；历史提交可追溯，但不能替代本次证据。

### 本次真实验证

- 启动本机 Java（local profile）、FastAPI、Vue，绑定回环地址；Java 连接实际 MySQL。
- 新注册独立演示用户：聊天“确认”不写入，专用确认每日时长 60→30，执行 SUCCEEDED；重复确认不新增执行、审计或设置副作用。
- 事件 1～14，游标 8 只返回 9～14，读取事件不新增业务审计。
- 该场景**没有调用模型、Tavily、Runner 或浏览器自动化**。无模型调用也能成功是业务工具的特性，不应伪装成模型验证。
- 详细命令、标识、全量回归结果见 [验收结果](agent-native-e2e-result.md)。

### 明确未完成 / 下一位开发者

- Task 26 尚缺独立、可复跑的 DeepSeek/Tavily/Qdrant→Vue→容器 Runner→Rubric→临时仓库 commit/push 全链路证据及演示记录。Task 22/23 历史实跑与本次新增覆盖分开看。
- Task 25 的 `InterfaceFallbackPolicy` 只有策略/预览，不能声称实际 Playwright 或 IDEA 动作已执行。
- 当前 events 是有限 SSE 格式事件重放，不是持续 token 级推送；不可把它写成长连接实时流式验收。
- 优先按结果报告的未验收清单补证据，发现缺实现则先写失败测试，不能直接转入付费体系。
- 本次改动不涉及数据库迁移、用户凭据或配置修改；演示账户仅用于本次验证，未删除用户数据。
- 本次提交主题：`fix: verify agent workflows against real contracts`；提交号可通过 `git log -1 --grep='verify agent workflows'` 定位。修改范围为上下文空状态修复、测试与验收文档，不包含本地配置。

## Task 25 工作记录

状态：部分完成；已有功能提交 `193e26e`。2026-09-08 更正：界面兜底只有策略预览，没有实际适配器，不能标记整个 Task 完成。

计划分为三个小步骤：

1. 根据工作区技术栈和变更文件，确定性推荐 Maven、npm、pytest 等白名单测试，并继续通过隔离 Runner 执行。
2. Git commit 与 Git push 建立两个互相独立的高风险动作；两者分别预览、分别确认，普通聊天文本不能代替确认接口。
3. 建立 API 优先的界面操作策略；只有能力目录确认没有业务 API 时，才允许调用预注册的 Playwright DOM 或 IDEA 无障碍动作，禁止任意 URL、选择器、脚本和键鼠操作。

### 已完成

- `WorkspaceDeveloperService`：增加测试推荐、commit/push 预览、摘要冲突保护、指定文件 commit 与 origin push。
- `AgentReadToolConfiguration` / `AgentWriteToolConfiguration`：发布推荐、预览、commit 与 push 类型化工具。
- `UnifiedAgentSupervisor`：支持“运行修改后的测试”“提交刚才修改”“推送刚才的提交”，多个工作区或多个改动文件时会澄清而不是猜测。
- `InterfaceFallbackPolicy`：业务 API 优先，只允许固定 Playwright/IDE 动作且拒绝任意操作目标。
- 前端与对接文档已同步 `GIT_COMMIT`、`GIT_PUSH` 执行类型和独立确认规则。

### 验证结果

- Java：361 项通过。
- AI 服务：302 项通过，Ruff 通过。
- Runner：21 项通过，Ruff 通过。
- 前端：124 项通过，TypeScript 与生产构建通过。
- `git diff --check`：通过。

### 已知边界

- 自动测试一次只提交推荐列表中的第一种模板，保证单轮最多一个写操作；其他技术栈需新开一轮执行。
- 界面兜底目前提供策略与白名单适配边界，不开放任意电脑控制。实际 Playwright/IDE 适配器必须遵循该注册表，并在 Task 26 真实环境验收。
- SSH push 依赖本机已有的合法 Git/SSH 配置；凭据不会进入模型、FastAPI 或 Runner 容器。

### 下一步

- 执行 Task 26 全栈真实联调；若由 Gemini 接手，先运行各服务健康检查，再按总计划的关键验收场景逐条验证。
- Task 26 完成后，将本节提交信息补充为最终提交号，并记录演示步骤与环境限制。
