# StudyPilot 后端开发说明

> 后端阶段版本：v0.1
>
> 技术栈：Java 17、Spring Boot 4、Spring Security、Spring Data JPA、MySQL、Flyway、Maven
>
> 代码目录：`backend/`

## 1. 后端阶段的边界

Java 后端已经作为 StudyPilot 的“业务事实来源”建立起来。它负责用户身份、数据隔离、学习目标、计划、任务、资料元数据、测验结果、Agent 授权和审计。后续 Python 服务负责模型调用、资料解析、RAG、联网搜索与 Agent 编排，但不能绕过 Java 直接修改核心业务数据库。

这种拆分允许先把稳定的业务规则建好，再让变化较快的 AI 能力通过受控接口接入。当前前端目录没有被修改。

## 2. 项目结构

```text
backend/src/main/java/com/moxiao/studypilot/
├── auth/          注册、登录、会话和内部服务认证
├── user/          学习偏好与每周可用时间
├── learning/      目标、计划、计划版本、任务和任务变更
├── material/      资料元数据与异步处理结果
├── assessment/    测验、作答和知识点掌握度
├── agent/         授权、执行状态、幂等和审计
├── notification/  站内通知
├── dashboard/     工作台聚合数据
└── shared/        统一错误响应和通用异常
```

每个业务模块大体按 `api / application / domain / infrastructure` 分层：

- `api` 定义 HTTP 输入输出，不直接写数据库；
- `application` 编排用例和事务；
- `domain` 保存枚举、领域约束和仓储接口；
- `infrastructure` 使用 JPA 实现持久化。

## 3. 功能与简要开发思路

### 3.1 账户、会话与数据隔离

- 注册时规范化邮箱并使用 BCrypt 保存密码哈希，不保存明文密码。
- 登录后生成 32 字节随机 Bearer Token，只将 Token 的 SHA-256 摘要保存到数据库。
- `GET /api/auth/me` 返回当前用户；退出登录会删除当前会话摘要，使旧 Token 立即失效。
- 业务表均保存 `owner_id`；查询和修改时同时使用资源 ID 与当前用户 ID，避免横向越权。

### 3.2 个人学习设置

- 保存时区、每日时长上限、周末偏好、默认资料隐私等级。
- 每周可用时间采用独立集合表保存星期、开始时间和结束时间。
- 写入前校验时区是否合法、结束时间是否晚于开始时间。

### 3.3 学习目标

- 支持创建、查询和修改目标，校验标题、未来截止日期以及每周 1～40 小时的学习投入。
- 新建目标默认是 `DRAFT`。
- 数据查询严格按当前用户隔离。

### 3.4 学习计划与版本

- 计划先以 `DRAFT` 创建，用户确认后才变为 `CONFIRMED`，符合“预览后写入正式计划”的产品规则。
- 每次创建和确认都会写入计划版本快照，前端可查询版本历史。
- Python Agent 已有内部接口创建计划草案，但不能替用户自动确认。

### 3.5 每日任务和变更记录

- 只有已确认的计划才能创建正式任务。
- 任务支持 `TODO / COMPLETED / SKIPPED / DEFERRED`。
- 完成任务会记录完成时间；延期必须提供新的安排日期。
- 每次状态或日期变化都会增加版本号并写入变更表，保存原状态、新状态、原日期、新日期和原因。

### 3.6 学习资料元数据

- 支持 Markdown、文本、PDF、网页、图片和粘贴文章等类型。
- 支持大纲、学习材料、个人笔记、参考资料四种用途，以及普通、敏感、仅本地三种隐私等级。
- 新资料只登记元数据并进入 `PENDING`，不会在上传请求中同步做耗时解析。
- Python 解析完成后通过内部接口回写状态、摘要、标签、知识点和内容存储引用；失败也会保存原因。

当前阶段不负责真实文件上传和解析，文件存储、OCR、网页抓取和向量化属于 Python/基础设施阶段。

### 3.7 测验与掌握度

- Python 可通过内部接口写入生成好的单选题或多选题、选项、标准答案、解析、知识点和模型名称。
- 面向用户的取题接口刻意不返回标准答案，提交后才返回判定与解析。
- 后端负责确定性判分，并按知识点维护历次正确率的滚动平均掌握度。
- 掌握度较低的知识点会进入工作台统计，为后续 Agent 插入复习任务提供依据。

### 3.8 Agent 授权、确认、幂等与审计

- 用户可创建带有效期的授权，范围包括资料处理、测验生成、小范围计划调整和大范围计划调整。
- 低风险执行只有在授权有效且包含所需范围时才进入 `PENDING`，否则等待授权。
- 高风险执行始终进入 `WAITING_CONFIRMATION`，必须由用户确认。
- `owner_id + idempotency_key` 建立唯一约束，避免打卡触发与夜间兜底重复创建同一调整。
- 执行记录保存触发方式、风险、状态、结果、失败原因、模型、Token 数、延迟和预估费用。
- 授权、执行创建、用户确认和状态变化都会写入审计日志。

### 3.9 站内通知与工作台

- Python 或后台流程可以通过内部接口创建资料就绪、计划调整、测验就绪、任务逾期和 Agent 失败通知。
- 用户可查看通知并标记已读。
- 工作台接口聚合目标数、今日任务数、今日完成数、待处理资料数、低掌握度知识点数和未读通知数，减少前端多次请求。

### 3.10 安全、错误与运行状态

- 用户接口使用 Bearer 会话认证；Python 内部接口使用独立的 `X-Internal-Service-Token`。
- 内部令牌为空时所有内部请求都会被拒绝，部署时必须显式配置。
- 参数错误、认证失败、资源不存在和业务冲突使用统一 JSON 错误结构。
- 默认允许 `http://localhost:5173` 跨域访问用户 API，可通过环境变量修改。
- `/actuator/health` 提供无敏感详情的公开健康检查。

## 4. 接口索引

用户接口需要 `Authorization: Bearer <token>`，注册和登录除外。

| 模块 | 方法与路径 | 用途 |
|---|---|---|
| 认证 | `POST /api/auth/register` | 注册并创建会话 |
| 认证 | `POST /api/auth/login` | 登录 |
| 认证 | `GET /api/auth/me` | 当前用户 |
| 认证 | `POST /api/auth/logout` | 注销当前会话 |
| 设置 | `GET /api/user-settings` | 查询学习设置 |
| 设置 | `PUT /api/user-settings` | 保存学习设置 |
| 目标 | `POST /api/learning-goals` | 创建目标 |
| 目标 | `GET /api/learning-goals` | 查询目标 |
| 目标 | `PATCH /api/learning-goals/{id}` | 修改目标 |
| 计划 | `POST /api/learning-plans` | 创建计划草案 |
| 计划 | `GET /api/learning-plans` | 查询计划 |
| 计划 | `POST /api/learning-plans/{id}/confirm` | 用户确认计划 |
| 计划 | `GET /api/learning-plans/{id}/versions` | 查询版本历史 |
| 任务 | `POST /api/learning-plans/{id}/tasks` | 为确认后的计划创建任务 |
| 任务 | `GET /api/learning-tasks?date=YYYY-MM-DD` | 查询全部或指定日期任务 |
| 任务 | `PATCH /api/learning-tasks/{id}/status` | 完成、跳过或延期 |
| 任务 | `GET /api/learning-tasks/{id}/history` | 查询变更历史 |
| 资料 | `POST /api/materials` | 登记资料 |
| 资料 | `GET /api/materials` | 查询资料 |
| 资料 | `GET /api/materials/{id}` | 查询资料详情 |
| 测验 | `GET /api/quizzes/{id}` | 获取不含答案的测验 |
| 测验 | `POST /api/quizzes/{id}/attempts` | 提交并判分 |
| 掌握度 | `GET /api/mastery` | 查询知识点掌握度 |
| Agent | `POST /api/agent-grants` | 创建授权 |
| Agent | `GET /api/agent-grants` | 查询授权 |
| Agent | `GET /api/agent-executions` | 查询执行记录 |
| Agent | `POST /api/agent-executions/{id}/confirm` | 确认高风险执行 |
| 审计 | `GET /api/audit-logs` | 查询审计日志 |
| 通知 | `GET /api/notifications` | 查询站内通知 |
| 通知 | `PATCH /api/notifications/{id}/read` | 标记已读 |
| 工作台 | `GET /api/dashboard` | 获取首页聚合统计 |

Python 服务使用 `X-Internal-Service-Token` 调用：

| 方法与路径 | 用途 |
|---|---|
| `GET /internal/users/{ownerId}/learning-context` | 获取 Agent 所需的结构化学习上下文 |
| `POST /internal/learning-plans` | 创建计划草案 |
| `PATCH /internal/materials/{id}/processing` | 回写资料解析结果 |
| `POST /internal/quizzes` | 写入生成后的测验 |
| `POST /internal/agent-executions` | 幂等创建 Agent 执行 |
| `PATCH /internal/agent-executions/{id}` | 回写执行状态和模型用量 |
| `POST /internal/notifications` | 创建站内通知 |

请求字段和枚举的精确定义以各模块 `api` 包中的 Java `record` 为准，前端联调时可直接据此生成 TypeScript 类型。

## 5. 数据库

Flyway 在应用启动时自动执行 `V1`～`V9`：

1. 用户和会话；
2. 学习目标；
3. 用户设置与每周可用时间；
4. 计划、计划版本、任务和任务变更；
5. 资料、标签和知识点；
6. 测验、题目、作答和掌握度；
7. Agent 授权、执行和审计；
8. 站内通知；
9. 任务延期历史字段。

JPA 使用 `ddl-auto=validate`，只校验实体与表结构，不在生产环境自动改表。数据库结构变化必须继续增加 Flyway 迁移文件。

## 6. 在 IntelliJ IDEA 中启动

1. 先安装 Docker Desktop，并启动 MySQL：

   ```bash
   docker compose --env-file infra/.env.example -f infra/docker-compose.yml up -d mysql
   ```

2. 打开 IDEA 的 `StudyPilotApplication` 运行配置，添加环境变量：

   ```text
   SPRING_PROFILES_ACTIVE=local
   INTERNAL_SERVICE_TOKEN=local-dev-internal-token
   ```

3. 运行 `StudyPilotApplication`。
4. 浏览器访问 `http://localhost:8080/actuator/health`，应得到 `{"status":"UP"}`。

也可以直接启动数据库和后端：

```bash
docker compose --env-file infra/.env.example -f infra/docker-compose.yml up --build
```

## 7. 测试

```bash
cd backend
./mvnw test
```

测试使用 H2 的 MySQL 兼容模式，不依赖本机数据库，覆盖：

- 注册、登录、当前用户和注销；
- 用户数据隔离；
- 设置、目标、计划确认、版本、任务状态和延期；
- 资料异步处理回写；
- 测验判分和掌握度；
- Agent 授权、确认、幂等和审计；
- 通知、工作台、内部工具契约；
- CORS 与健康检查。

## 8. 留给 Python 阶段的工作

后端已经预留契约，但以下能力尚未实现，下一阶段需要共同设计：

- FastAPI 服务骨架与 Java 内部接口客户端；
- 云端模型 API 主路由和 Ollama 备用路由；
- PDF、网页、图片/OCR 解析与对象存储；
- 文本切分、Embedding、向量库和可追溯 RAG；
- 联网搜索、来源冲突处理和引用保存；
- 对话流式输出和 Agent 状态机；
- 根据掌握度、打卡和夜间检查生成计划调整建议；
- 模型调用限额、重试、降级和 Agent 评测集。

Redis、消息队列和向量数据库没有为了“技术栈丰富”而提前加入。等 Python 阶段明确异步任务与检索方案后再选型，可以避免重复改造。
