# StudyPilot 项目开发步骤

> 这是一份持续更新的项目总览，用来回答三个问题：项目分成多少阶段、当前开发到
> 哪里、每个小功能具体做了什么。
>
> 状态说明：`✅ 已完成`、`🚧 开发中`、`⬜ 未开始`。

## 一、总体进度

StudyPilot 采用两层迭代模型：核心交付阶段 0～8 已全部完成；其后通过独立里程碑继续
演进。阶段 9 交互式课程已经完成并作为兼容历史保留，Roadmap Foundation 已完成并
成为新的主要学习入口；下一里程碑是 Roadmap Learning Loop。

| 阶段 | 名称 | 状态 | 阶段产出 |
|---|---|---|---|
| 0 | 项目定位与工程基线 | ✅ 已完成 | 产品范围、单仓库、Git 与基础运行环境 |
| 1 | Java 传统业务后端 | ✅ 已完成 | 用户、目标、计划、任务、资料、测验、审计 |
| 2 | Python AI 服务基础 | ✅ 已完成 | FastAPI、DeepSeek、内部认证、Java 客户端 |
| 3 | 学习计划 Agent 闭环 | ✅ 已完成 | 多轮对话、草稿修改、人工确认、原子落库 |
| 4 | 每日任务执行闭环 | ✅ 已完成 | 今日任务、打卡、对话式操作、历史记录 |
| 5 | 自适应计划调整 | ✅ 已完成 | 根据执行偏差提出并执行计划调整 |
| 6 | 资料读取与联网搜索 | ✅ 已完成 | 资料解析、检索、搜索、来源引用 |
| 7 | 测验与掌握度闭环 | ✅ 已完成 | 出题、作答、判分、薄弱点复习 |
| 8 | 前端联调与工程化发布 | ✅ 已完成 | Vue 联调、持久记忆、部署、评测与演示 |

| 扩展里程碑 | 状态 | 定位 |
|---|---|---|
| 阶段 9：交互式课程学习平台 | ✅ 已完成 | Course/Lesson 教学闭环，现保留为直接 URL 兼容历史 |
| Roadmap Domain Foundation | ✅ 已完成 | 12 阶段、64 节点的主要学习入口与状态基础 |
| Roadmap Learning Loop | 🚧 开发中 | 学习主闭环已完成，正在建设成果验收与安全 Runner |

当前开发位置：

```text
核心交付阶段 0～8 ✅
          ↓
阶段 9：交互式课程学习平台 ✅（兼容历史）
          ↓
Roadmap Domain Foundation ✅（主要学习入口）
          ↓
Roadmap Learning Loop 🚧（Task 11：成果登记已完成）
```

---

## 二、已经完成的开发步骤

### 阶段 0：项目定位与工程基线（4 步）

#### 0.1 产品方向与 MVP

- 状态：✅ 已完成
- 开发内容：确定个人 AI 学习执行工作台方向，先服务个人学习，再逐步增加 Agent
  代操作能力。
- 主要产出：产品需求、MVP 边界、技术路线。

#### 0.2 Java + Python 架构

- 状态：✅ 已完成
- 开发内容：Java 作为业务事实与权限中心；Python 负责模型调用、Agent 编排和
  检索；Python 不直接写 MySQL。
- 主要产出：模块职责边界与内部 HTTP 契约。

#### 0.3 单仓库结构

- 状态：✅ 已完成
- 开发内容：建立 `backend/`、`ai-service/`、`web/`、`infra/`、`docs/`。
- 主要产出：可以分别开发又方便联调的 Monorepo。

#### 0.4 Git 与 GitHub

- 状态：✅ 已完成
- 开发内容：配置本地 Git、提交规范和 GitHub SSH 远程仓库。
- 验收结果：代码可以持续提交并推送到 `main`。

### 阶段 1：Java 传统业务后端（10 步）

#### 1.1 用户注册、登录与会话

- 状态：✅ 已完成
- 开发内容：BCrypt 密码哈希、随机 Bearer Token、会话摘要、注销。
- 验收结果：用户可以注册、登录并访问受保护接口。

#### 1.2 用户数据隔离

- 状态：✅ 已完成
- 开发内容：所有核心资源绑定 `owner_id`，查询和修改同时校验资源与用户。
- 验收结果：不同用户不能读取或修改彼此数据。

#### 1.3 学习设置

- 状态：✅ 已完成
- 开发内容：时区、每日时长、周末偏好、资料隐私和每周可用时间。

#### 1.4 学习目标

- 状态：✅ 已完成
- 开发内容：创建、查询、修改目标，校验标题、截止日期和每周时长。

#### 1.5 学习计划与版本

- 状态：✅ 已完成
- 开发内容：计划先创建为 `DRAFT`，用户确认后变为 `CONFIRMED`，并保存版本快照。

#### 1.6 每日任务与变更历史

- 状态：✅ 已完成
- 开发内容：任务支持 `TODO / COMPLETED / SKIPPED / DEFERRED`；状态和日期变化写入
  `task_changes`。

#### 1.7 学习资料元数据

- 状态：✅ 已完成
- 开发内容：资料类型、用途、隐私、处理状态、摘要、标签和知识点。

#### 1.8 测验与掌握度

- 状态：✅ 已完成
- 开发内容：题目、选项、答案、作答判分以及知识点掌握度统计。

#### 1.9 Agent 治理

- 状态：✅ 已完成
- 开发内容：授权范围、风险等级、显式确认、幂等执行记录和审计日志。

#### 1.10 通知、工作台与测试

- 状态：✅ 已完成
- 开发内容：通知、首页聚合统计、统一错误、健康检查和完整接口测试。

### 阶段 2：Python AI 服务基础（6 步）

#### 2.1 FastAPI 服务骨架

- 状态：✅ 已完成
- 开发内容：应用入口、健康检查、模块化目录和 Python 3.12 虚拟环境。

#### 2.2 配置与密钥

- 状态：✅ 已完成
- 开发内容：通过 `.env` 和 Pydantic Settings 管理 DeepSeek Key，不把密钥提交到
  Git。

#### 2.3 内部服务认证

- 状态：✅ 已完成
- 开发内容：Java 与 Python 使用相同的 `X-Internal-Service-Token`。

#### 2.4 DeepSeek 模型适配

- 状态：✅ 已完成
- 开发内容：通过 OpenAI 兼容接口接入 DeepSeek V4，保留未来 Ollama 替换点。

#### 2.5 Java 类型化客户端

- 状态：✅ 已完成
- 开发内容：使用 HTTPX 和 Pydantic 调用 Java 内部接口，集中处理令牌、超时和错误。

#### 2.6 自动化测试基线

- 状态：✅ 已完成
- 开发内容：Pytest、Ruff、Fake Model 与 MockTransport，自动测试不消耗模型 Token。

### 阶段 3：学习计划 Agent 闭环（8 步）

#### 3.1 读取学习上下文

- 状态：✅ 已完成
- 开发内容：Python 从 Java 读取目标、计划、任务、资料元数据和掌握度。

#### 3.2 结构化计划模型

- 状态：✅ 已完成
- 开发内容：Pydantic 校验计划日期、任务日期、任务数量和学习时长。

#### 3.3 DeepSeek Planner

- 状态：✅ 已完成
- 开发内容：模型只能返回 `COLLECTING` 或 `DRAFT_READY` JSON；提示词包含完整结构
  示例。

#### 3.4 LangGraph 多轮上下文

- 状态：✅ 已完成
- 开发内容：同一 `conversationId` 保留对话，不同会话互相隔离。

#### 3.5 草稿修改

- 状态：✅ 已完成
- 开发内容：用户可以继续对话修改草稿，修改阶段不写正式计划。

#### 3.6 人工确认

- 状态：✅ 已完成
- 开发内容：LangGraph 在写操作前 `interrupt`，只有独立 `confirm` 接口可以恢复。

#### 3.7 Java 原子落库

- 状态：✅ 已完成
- 开发内容：单个事务创建 `CONFIRMED` 计划和全部任务，幂等键避免重复写入。

#### 3.8 真实联调

- 状态：✅ 已完成
- 开发内容：完成多轮提问、两次草稿修改、确认、MySQL 写入和审计核验。
- 验收结果：保存 1 个正式计划、7 个任务、600 分钟学习时间；AgentExecution 为
  `SUCCEEDED`。

---

## 三、当前阶段：每日任务执行闭环（6 步）

本阶段目标：让用户不仅能生成计划，还能每天使用任务，并让 Agent 在授权范围内
代替用户完成确定性的任务操作。

### 4.1 Java 内部今日任务查询

- 状态：✅ 已完成
- 用户行为：Agent 可以读取指定用户某一天的学习任务。
- 开发方法：复用 Java `LearningTaskService` 的用户隔离查询，新增受内部令牌保护的
  只读接口。
- 已实现接口：

  ```text
  GET /internal/users/{ownerId}/learning-tasks?date=YYYY-MM-DD
  ```

- 验收标准：只返回该用户指定日期的任务；其他用户任务不会泄露；不修改数据库。
- 关键文件：
  - `InternalLearningToolController.java`：内部只读 HTTP 入口。
  - `InternalDailyTaskQueryContractTest.java`：认证、日期过滤和用户隔离契约测试。
- 验证结果：
  - 正确内部令牌返回指定日期任务。
  - 缺少内部令牌返回 401。
  - 非 ISO 日期返回 400。
  - 两个用户的同日任务不会混合。
  - Java 全量 30 个测试全部通过。
- 对应提交：`feat: expose internal daily task query`。

### 4.2 Java 幂等任务状态工具

- 状态：✅ 已完成
- 用户行为：Agent 可以把确定的任务标记为完成、跳过或延期。
- 开发方法：新增内部请求模型、任务归属校验、操作幂等键和预期版本校验；继续复用
  现有任务状态机与任务变更历史。
- 已实现接口：

  ```text
  PATCH /internal/learning-tasks/{taskId}/status
  ```

- 关键设计：
  - `idempotencyKey` 防止网络重试造成重复执行。
  - `expectedVersion` 防止 Agent 使用旧任务快照覆盖用户的新操作。
  - `ownerId + taskId` 校验防止跨用户修改任务。
  - MySQL 唯一索引是并发场景下的最终防重边界。
  - 普通用户任务接口仍可继续写入不带幂等键的历史记录。
- 关键文件：
  - `InternalChangeTaskStatusRequest.java`：定义 Agent 写操作的严格请求契约。
  - `LearningTaskService.java`：执行归属、幂等和乐观版本检查。
  - `TaskChangeEntity.java`：保存操作幂等键和任务变更历史。
  - `V11__add_task_operation_idempotency.sql`：增加可空幂等键与唯一索引。
  - `InternalTaskStatusToolContractTest.java`：覆盖重复调用、键冲突、旧版本、
    越权和延期约束。
- 验证结果：
  - 同一个请求执行两次，任务版本只从 1 增加到 2，历史记录只有一条。
  - 同一幂等键用于不同操作返回 409。
  - Agent 使用过期任务版本返回 409，操作其他用户任务返回 404。
  - 延期缺少日期、缺少原因或延期到今天均返回 400。
  - 修正了原状态机允许“延期到今天”的边界问题。
  - Java 全量 33 个测试全部通过。
  - Flyway 已在本机 MySQL 上从 V10 升级到 V11，健康检查为 `UP`。
- 对应提交：`feat: add idempotent agent task status tool`。

### 4.3 Python 任务工具客户端

- 状态：✅ 已完成
- 用户行为：Python 可以查询任务并调用 Java 的受控状态修改工具。
- 开发方法：新增 Pydantic 契约和 HTTPX 方法，使用 MockTransport 验证 camelCase
  JSON、内部令牌和错误转换。
- 已实现方法：

  ```python
  await client.get_learning_tasks(owner_id, target_date=target_date)
  await client.change_learning_task_status(task_id, request)
  ```

- 关键设计：
  - 使用 `LearningTaskStatus` 枚举统一 Java/Python 任务状态。
  - `ChangeLearningTaskStatusRequest` 限制幂等键长度、任务版本和原因长度。
  - Python 自动把 snake_case 请求转换成 Java camelCase JSON。
  - 查询日期由调用方显式传入，避免“今天”受服务器时区影响。
  - 客户端不自行生成幂等键，由后续 Agent 编排层确定一次用户操作的稳定身份。
  - `JavaBackendError` 保留 `status_code`、`detail` 和 `path`，后续 Agent 可区分
    400、404、409、服务异常和连接失败。
  - 本步没有新增公开 FastAPI 写接口，不能绕过后续的用户确认流程。
- 关键文件：
  - `app/schemas/learning.py`：任务状态枚举和状态修改请求契约。
  - `app/clients/java_backend.py`：任务查询、状态修改和错误转换。
  - `tests/clients/test_java_backend.py`：Java/Python HTTP 契约测试。
- 验证结果：
  - 客户端局部测试 15 个全部通过。
  - Python 全量 37 个测试全部通过。
  - Ruff 静态检查通过。
  - 测试使用 `httpx.MockTransport`，没有启动 Java、访问 MySQL 或调用 DeepSeek。
- 对应提交：`feat: add python task tool client`。

### 4.4 对话式任务识别

- 状态：✅ 已完成
- 用户行为：用户可以说“我完成了今天的 Spring Boot 任务”。
- 开发方法：模型只负责输出候选任务和意图；Java 任务 ID、用户归属和状态转换仍由
  确定性代码校验。
- 已识别意图：
  - `LIST_TASKS`：查询指定日期任务。
  - `COMPLETE_TASK`：生成完成任务候选。
  - `SKIP_TASK`：生成带原因的跳过候选。
  - `DEFER_TASK`：生成带原因和未来日期的延期候选。
  - `UNKNOWN`：无法识别或不属于任务操作。
- 关键设计：
  - 任务识别工作流与现有学习计划工作流分离，未来可由统一聊天路由分发。
  - DeepSeek 只返回 Pydantic 结构化识别结果，不能直接调用 Java 写接口。
  - 提示词把用户消息和任务标题标记为不可信数据，并禁止展示思维链或声称已执行。
  - 模型只能返回 Java 候选列表中的任务 ID；伪造 ID 会被确定性代码拒绝。
  - 多个候选、没有候选、已结束任务、缺少原因或非法延期日期都会要求继续澄清。
  - 只有唯一合法候选会形成包含任务 ID 和 `expectedVersion` 的只读操作草稿。
  - 本阶段没有 FastAPI 写入口，也不会调用任务状态 PATCH 接口。
- 关键文件：
  - `app/agent/task_models.py`：意图、解析状态、候选任务和只读操作草稿。
  - `app/prompts/task_action.py`：任务识别提示词与不可信数据边界。
  - `app/agent/task_recognizer.py`：DeepSeek 结构化识别和一次修正重试。
  - `app/agent/task_service.py`：Java 任务查询与确定性安全解析。
  - `tests/agent/test_task_*.py`：模型、识别器和应用服务测试。
- 验证结果：
  - 4.4 新增 20 个自动化测试全部通过。
  - Python 全量 57 个测试全部通过。
  - Ruff 静态检查通过。
  - 测试使用假模型和只读 Fake Java 客户端，没有调用 DeepSeek、Java 写接口或 MySQL。
- 对应提交：`feat: recognize conversational task actions`。

### 4.5 操作预览、授权与确认

- 状态：✅ 已完成
- 用户行为：执行前看到“将完成哪个任务”，明确确认后才修改。
- 开发方法：复用 AgentExecution、RiskLevel、Scope、LangGraph interrupt 和审计。
- 已实现接口：

  ```text
  POST /internal/agent/task-conversations
  POST /internal/agent/task-conversations/{id}/messages
  GET  /internal/agent/task-conversations/{id}
  POST /internal/agent/task-conversations/{id}/confirm
  ```

- 关键设计：
  - Java 新增 `TASK_STATUS_CHANGE` 执行类型和 `TASK_MANAGEMENT` 授权范围。
  - 所有任务写操作固定为 `HIGH` 风险，每次都必须通过专用确认接口。
  - LangGraph 在生成预览后使用 `interrupt` 暂停，普通聊天文本不能代替授权。
  - 预览阶段继续发送消息会重新识别并替换草稿，不会修改任务。
  - 执行记录和任务写入共用 `task-action:{conversationId}` 稳定幂等键。
  - 确认后依次记录 `PENDING → RUNNING → SUCCEEDED` 并调用 Java 任务工具。
  - Java 409 会将会话和执行记录置为 `FAILED`，要求重新查询最新任务。
  - 重复确认已完成会话直接返回原快照，不重复写任务历史。
  - 当前使用内存 Checkpointer，服务重启后会话丢失，阶段 8 再持久化。
- 关键文件：
  - `task_conversation_graph.py`：识别、预览、interrupt 和执行图。
  - `task_conversation_service.py`：会话身份、并发锁、确认和错误转换。
  - `task_conversations.py`：四个受内部令牌保护的 FastAPI 接口。
- 验证结果：
  - Java 全量 34 个测试全部通过。
  - Python 全量 73 个测试全部通过。
  - Ruff 静态检查通过。
  - 自动测试验证未确认零写入、确认后只写一次、修改预览和版本冲突失败审计。
- 对应提交：`feat: execute confirmed task actions`。

### 4.6 端到端打卡联调

- 状态：✅ 已完成
- 用户行为：对话查询今日任务 → 选择任务 → 确认完成 → 查询历史。
- 开发方法：
  - 新增独立 `docs/task-agent-e2e.http`，可以在 IDEA 中从上到下重复联调。
  - 新增 API 级自动化闭环测试，覆盖创建、预览、查询、确认和重复确认。
  - 本机真实启动 Spring Boot、FastAPI、MySQL，并调用已配置的 DeepSeek。
  - 通过 Java 用户接口查询最终任务、任务历史、执行记录和审计日志。
- 真实联调结果（2026-07-26）：
  - 自然语言识别后会话进入 `PREVIEW_READY`。
  - 确认前任务保持 `TODO`、version 1，证明预览阶段没有提前写入。
  - 专用确认接口执行后，会话和任务均为 `COMPLETED`，任务 version 为 2，
    `completedAt` 已写入。
  - `task_changes` 只有一条，AgentExecution 为 `SUCCEEDED`。
  - 审计日志包含 `EXECUTION_CREATED`、`EXECUTION_CONFIRMED` 和
    `EXECUTION_STATUS_CHANGED`。
  - 再次调用确认接口后任务仍为 version 2，历史仍只有一条。
- 本机启动要点：

  ```bash
  cd backend
  set -a; source ../ai-service/.env; set +a
  ./mvnw spring-boot:run -Dspring-boot.run.profiles=local

  cd ai-service
  set -a; source .env; set +a
  .venv/bin/python -m uvicorn app.main:app --reload --port 8000
  ```

  Java 和 FastAPI 必须读取同一个 `INTERNAL_SERVICE_TOKEN`；如果只启动 Java 而未
  注入该环境变量，内部接口会返回 401。
- 验证结果：
  - Java 全量 34 个测试通过。
  - Python 全量 74 个测试通过。
  - Ruff 静态检查通过。
- 已知限制：任务会话和 LangGraph Checkpointer 仍在内存中，FastAPI 重启后会话
  丢失；该能力计划在阶段 8 持久化。
- 对应提交：`test: complete task agent end-to-end workflow`。

---

## 四、当前阶段：自适应计划调整（5 步）

### 阶段 5：自适应计划调整（5 步）

#### 5.1 记录真实学习时长

- 状态：✅ 已完成
- 用户行为：完成任务时可以同时记录 `actualMinutes`，省略时保持兼容。
- 开发方法：Java 负责字段校验与持久化，Python 任务 Agent 可以从自然语言提取
  实际分钟数；只有 `COMPLETED` 状态允许写入 1～720 分钟。

#### 5.2 聚合执行偏差

- 状态：✅ 已完成
- 用户行为：系统能够根据真实打卡结果识别计划是否需要调整。
- 确定性规则：
  - `OVERDUE_TASKS`：到分析日仍为 TODO 的逾期任务。
  - `CONSECUTIVE_SKIPS`：最近执行序列连续跳过至少 2 次。
  - `TIME_ESTIMATE_BIAS`：至少 3 条实际时长样本，聚合偏差超过 30%。
- 已实现接口：

  ```text
  GET /internal/users/{ownerId}/adaptation-context
      ?analysisDate=YYYY-MM-DD&windowDays=14
  ```

#### 5.3 生成并持久化调整草稿

- 状态：✅ 已完成
- 用户行为：DeepSeek 根据偏差信号提出重新安排、修改预计时长或拆分任务的候选。
- 安全边界：模型不能直接写数据库；Python 校验真实任务 ID、TODO 状态、版本、
  日期和每日学习上限，Java 再重新判定风险并持久化草稿。
- 风险规则：最多 3 个操作、最多 1 次拆分且不延长计划周期为 `LOW`，否则升级为
  `HIGH`。

#### 5.4 授权、确认与原子执行

- 状态：✅ 已完成
- 用户行为：有 `SMALL_PLAN_ADJUSTMENT` 长期授权时小调整自动执行；无授权时等待
  授权；大调整始终逐次确认。
- 已实现接口：

  ```text
  POST /internal/agent/plan-adjustments/analyze
  GET  /internal/agent/plan-adjustments/{adjustmentId}
  POST /internal/agent/plan-adjustments/{adjustmentId}/confirm
  POST /internal/plan-adjustments/{adjustmentId}/execute
  ```

- 关键设计：
  - 全部任务版本先校验，再在一个 Java 事务中修改，避免部分成功。
  - 一次调整无论包含多少操作，计划版本只增加一次。
  - 完整前后快照保存在调整记录中，新版本写入计划版本历史。
  - 成功、失败、等待确认都有通知；执行状态变化进入审计日志。

#### 5.5 午夜补偿、幂等和真实联调

- 状态：✅ 已完成
- 用户行为：APScheduler 每 15 分钟查询一次待分析用户；错过零点或服务重启后仍能
  根据用户时区补跑。
- 幂等规则：夜间键为
  `plan-adjustment:nightly:{ownerId}:{analysisDate}`；手动分析也在调用模型前查询
  Java 幂等记录，重复请求不再次消耗模型、不执行第二次。
- 真实联调结果：
  - 本机 MySQL 真实应用 V12、V13 迁移。
  - Java 聚合出 `OVERDUE_TASKS` 和 `TIME_ESTIMATE_BIAS`。
  - DeepSeek 生成 `LOW` 调整，长期授权后自动完成。
  - 计划版本从 2 增加到 3，版本历史恰好 3 条。
  - 调整前后快照完整，通知为 `PLAN_ADJUSTED`。
  - AgentExecution 为 `SUCCEEDED`，审计包含 `RUNNING` 和 `SUCCEEDED`。
  - 重复分析返回同一调整记录，版本仍为 3。
- 自动化验证：
  - Java 全量 43 个测试通过。
  - Python 全量 87 个测试通过。
  - Ruff 与 `git diff --check` 通过。
- 联调中修复：
  - V13 外键误写为 `user_accounts`，修正为真实表 `app_users`。
  - 调整前快照重复插入相同计划版本导致 MySQL 唯一键冲突，改为只在调整记录保存
    before snapshot，版本表只新增调整后的版本。
  - 提示词缺少精确 JSON Schema 导致模型结构不稳定，现已注入 camelCase Schema。
- 独立联调文件：`docs/plan-adjustment-e2e.http`。
- 已知限制：模型偶发输出仍可能无法通过业务校验，系统会安全返回 422 且不执行；
  调整会话没有单独的自然语言修改流程，后续由前端表单承接微调。

---

## 五、当前阶段：资料读取与联网搜索（6 步）

### 阶段 6：资料读取与联网搜索（6 步）

#### 6.1 资料导入、存储与异步解析

- 状态：✅ 已完成
- 支持 TXT、Markdown、PDF、DOCX、粘贴文本和网页 URL，限制单文件 20 MB。
- Java 保存原文件并提供内部下载接口；Python 使用带租约的 MySQL 任务领取、
  心跳、失败与最多三次重试，不依赖 Redis/Celery。
- URL 导入和重定向均校验公网 HTTP/HTTPS 地址，拦截回环、内网和链路本地地址。
- 对应提交：`87d0b4a feat: import and parse learning materials`。

#### 6.2 分段、摘要与知识点

- 状态：✅ 已完成
- PDF 保留页码，DOCX 读取段落和表格；扫描 PDF 返回“需要 OCR”的明确错误。
- 默认约 450 字符、60 字符重叠，MySQL 原子保存文本块、定位、摘要、标签、
  知识点与警告。
- `SENSITIVE`、`LOCAL_ONLY` 不向 DeepSeek 发送正文，只做本地解析、分段和索引。
- 对应提交：`eb9a038 feat: extract structured material knowledge`。

#### 6.3 本地混合检索

- 状态：✅ 已完成
- Qdrant 本地持久化保存派生索引，MySQL 始终是规范数据源。
- FastEmbed 同时生成多语言 dense 向量和 BM25 sparse 向量，再用 RRF 融合。
- dense、sparse 和最终查询三层均强制 `ownerId` 过滤；`SYLLABUS` 在结果中优先。
- 对应提交：`25d5335 feat: retrieve materials with hybrid search`。

#### 6.4 联网搜索与确认入库

- 状态：✅ 已完成
- Tavily 只返回检索结果，不使用供应商生成的答案；Java 保存 query、URL、片段、
  得分和 request ID。
- 搜索结果默认只用于当前回答，只有用户调用确认导入接口后才创建 `WEB_PAGE`
  资料并进入解析队列。
- Key 缺失或供应商失败时继续使用本地资料，并返回真实降级警告。
- 对应提交：`e6e91c1 feat: search web with traceable sources`。

#### 6.5 多轮知识问答

- 状态：✅ 已完成
- 新增创建、发消息和查询知识会话三个内部接口；每轮重新检索并保留可见历史。
- AUTO 根据版本、最新、官方 API 等时效词决定是否联网，也支持显式启用或禁用。
- 回答返回 MATERIAL/WEB 引用、定位、片段和降级警告。
- 只要召回隐私资料，本轮就不调用 DeepSeek 或 Tavily，直接返回本地相关片段。
- 当前会话在内存中，SSE 与持久化留到阶段 8。
- 对应提交：`469a1a1 feat: answer grounded knowledge questions`。

#### 6.6 计划增强与端到端闭环

- 状态：✅ 已完成
- 计划 Agent 每个用户回合重新检索，并把来源引用随计划草稿返回。
- 提示词固定优先级：用户当前约束 > SYLLABUS > 普通资料与联网结果 > 模型常识；
  大纲控制学习顺序，最新事实仍以可靠网页为准，冲突必须明确展示。
- 隐私资料正文不会进入计划模型上下文；命中时返回警告。
- 独立联调文件：`docs/material-rag-e2e.http`，覆盖资料导入、异步解析、连续问答、
  联网降级/搜索、确认网页入库以及计划增强。
- 自动化验证：
  - Java 全量 48 个测试通过。
  - Python 全量 117 个测试通过。
  - Ruff 与 `git diff --check` 通过。
- 本机真实联调（2026-07-26）：
  - MySQL 成功应用 V14～V16，资料大纲从 `PENDING` 进入 `READY`，DeepSeek 生成
    摘要与标签，FastEmbed/Qdrant 完成首次本地索引。
  - 知识问答返回大纲的 `正文 / chunk 1` 引用；连续时效问题仍重新检索。
  - 计划 Agent 返回 `DRAFT_READY`，任务从 Java 基础开始并携带大纲引用。
  - Tavily Key 尚未配置，时效问题返回“未执行联网搜索”的真实降级警告；配置 Key
    后可按联调文件继续验证 WEB 引用和确认入库。
- 已知限制：OCR、SSE、Qdrant Server、Docker、Ollama 本地摘要和会话持久化留到
  阶段 8；本地 Qdrant 仅支持单进程共享客户端。
- 对应提交：`test: complete material rag end-to-end workflow`。

---

## 六、后续阶段概览

### 阶段 7：测验与掌握度闭环（4 步）

1. ✅ 基于任务、资料和必要网页生成固定五题，按掌握度切换三档题型与难度；每题
   保存可追溯来源，稳定基础知识可显式标记 `MODEL_KNOWLEDGE`。
2. ✅ Java 精确判定单选和多选；编程答案进入 MySQL 租约队列，由 DeepSeek 仅做
   文本逻辑评估，保留幂等键、多次 attempt 历史和失败后的部分评分。
3. ✅ 记录 QUIZ、TASK、SELF_ASSESSMENT 三类证据，采用近期 EWMA 和 80/15/5
   缺省归一化；编程题在测验信号中的权重为 0.3。
4. ✅ 每次最终评分选择最多两个低于 70 的薄弱点，生成 REVIEW 或
   CODING_PRACTICE 候选；任务插入复用计划调整授权、确认、版本、通知与审计。

- 数据库迁移：V17～V20。
- 联调文件：`docs/quiz-mastery-e2e.http`。
- 已知限制：代码不执行；评分属于 AI 学习反馈。测验生成需要模型，Tavily 未配置
  时只对联网部分明确降级。
- 验证结果（2026-07-27）：
  - Java Maven 全量 55 项通过；Python pytest 133 项通过；Ruff 与
    `git diff --check` 通过。
  - MySQL 从 V16 实际迁移到 V20；真实 DeepSeek 生成 5 题并保存到 Java。
  - 真实代码文本评估进入 `GRADED`，返回 `AI_EVALUATED` 和未执行代码警告。
  - 掌握度证据、自评重算及最低两个薄弱点的 LOW 治理候选均已验证。
  - Tavily 本轮未启用，使用 `MODEL_KNOWLEDGE` 验证无本地资料时的稳定基础题降级。
- 对应提交：
  - `feat: generate grounded adaptive quizzes`
  - `feat: evaluate quiz attempts and coding answers`
  - `feat: calculate weighted knowledge mastery`
  - `test: complete adaptive quiz learning loop`

### 阶段 8：前端联调与工程化发布（6 步）

#### 8.1 Java 认证 Agent 公共门面

- 状态：✅ 已完成
- 用户行为：Vue 只携带 Bearer Token 调用 `/api/agent/**`；Java 自动注入当前用户，
  支持计划、任务、知识问答、计划调整和测验生成。
- 实现方法：Java 统一内部 HTTP 客户端、120 秒模型超时、错误码映射和返回 owner
  边界检查；Python 对读取、消息与确认再次校验 owner。
- 测试：覆盖未认证、owner 隔离、超时、错误映射及内部响应泄漏。
- 提交：`feat: expose authenticated agent facade`，以及 3 个契约加固提交。

#### 8.2 Vue 真实 Agent 联调

- 状态：✅ 已完成
- 用户行为：计划、任务、知识问答、调整和测验页面默认访问真实后端；会话 ID 保存在
  URL，刷新可恢复。`DRAFT_READY` 用结构化表单修改，专用按钮才会确认写入。
- 实现方法：统一 `HttpAgentGateway`、120 秒请求、错误与空来源提示；模型身份显示
  Java/Python 返回的真实 provider/model，不使用硬编码回答。
- 测试：覆盖 HTTP gateway、刷新恢复、草稿差异、确认状态和模型身份。
- 提交：`feat: connect Vue to live agent workflows`，以及 2 个交互加固提交。

#### 8.3 用户 AI Key 安全存储

- 状态：✅ 已完成
- 用户行为：设置页可以新增或删除 DeepSeek/Tavily Key，只显示来源与脱敏尾号；
  开发环境可回退到服务器 Key。
- 实现方法：Java 使用随机 IV 的 AES-GCM 加密，AAD 绑定 owner 与 provider，MySQL
  只保存密文；Python 按请求租赁用户运行时，后台任务同样遵守用户 Key 优先级。
- 测试：覆盖加密往返、篡改失败、owner 隔离、缓存失效、回退和安全审计。
- 提交：`feat: secure per-user AI credentials`，以及 4 个生命周期加固提交。

#### 8.4 LangGraph 会话持久化

- 状态：✅ 已完成
- 用户行为：FastAPI 重启后可以按 `conversationId` 恢复计划、任务与知识会话并继续
  消息或确认。
- 实现方法：`AsyncSqliteSaver` 保存 Checkpoint，同一 SQLite 保存加密会话元数据、
  快照和历史；启动时恢复会话锁，并以 OS 文件锁强制单进程。
- 测试：覆盖重启恢复、owner 校验、密钥错误、并发初始化、关闭清理和单进程锁。
- 提交：`feat: persist agent conversations and checkpoints`，以及 6 个故障安全加固提交。

#### 8.5 Docker、日志、限流与监控

- 状态：✅ 已完成
- 用户行为：Compose 可以启动 MySQL、Java、单进程 FastAPI 和 Vue/Nginx；浏览器同源
  访问 Java，不接触 Python internal。可选 Prometheus 查看模型调用指标。
- 实现方法：命名卷保存业务/会话/索引/缓存；请求关联 ID、秘密安全日志、单用户
  Agent 30 次/分钟和模型消息 10 次/分钟限流。
- 测试：Compose/Nginx/Dockerfile 静态契约、限流、关联 ID、指标和日志脱敏。
- 提交：`chore: package full local deployment stack`，以及容器与观测加固提交。

#### 8.6 发布验收、演示与文档

- 状态：✅ 已完成
- 用户行为：提供不含秘密的公共 API 联调文件、可重复演示数据脚本、原生/Compose
  部署指南和五分钟演示路线。
- 实现方法：独立端口执行原生端到端验收，验证模型身份、资料引用、计划/任务确认、
  测验和 FastAPI 重启恢复；脚本只通过 Java 公共接口创建数据。
- 验证：Java Maven、Python pytest/Ruff、Vue test/typecheck/build、
  `git diff --check` 均执行；真实结果见 `docs/stage8-e2e-result.md`。
- 已知限制：Docker 未安装时仅验证静态契约；SQLite/Qdrant 单进程；无 SSE、OCR、
  Ollama、多人协作或真实代码执行。
- 提交：`docs: complete studypilot stage eight release`。

### 阶段 8 后续优化：Java + AI 垂直学习范围

- 状态：✅ 已完成
- 用户行为：StudyPilot 现在只围绕 moxiao 的 Java + AI 项目技术栈回答、搜索和生成
  计划，不再主动扩展为泛学科学习平台。
- 实现方法：
  - Python 新增集中式学习范围策略，统一约束计划提示词、知识问答和联网查询。
  - 固定范围为 Java、Spring Boot、MySQL、Vue/TypeScript、Python/FastAPI、
    DeepSeek API、LangChain/LangGraph、RAG/Qdrant/Tavily、Git/Docker。
  - 教程、课程、视频、资料和路线类问题在 AUTO 模式下主动联网，查询优先加入
    “黑马程序员 itheima B站”；当前版本、兼容性和 API 问题保持原查询并优先官方
    文档。
  - 新建 `docs/studypilot-java-ai-starter-syllabus.md`，按九个阶段记录项目对应模块、
    最低目标、黑马主资料、官方补充与暂不深入内容。
- 资料入库（2026-07-30）：
  - 标题：`StudyPilot Java + AI 基础学习路线`
  - 分类/隐私：`SYLLABUS / NORMAL`
  - 资料 ID：`253355f1-3114-4d1a-8a53-9f79ffd67b23`
  - 处理状态：`READY`，已完成摘要、标签、分段与 Qdrant 索引。
- 真实联调：
  - 问题“推荐 Spring Boot 学习视频”触发 Tavily 搜索。
  - 返回 `HYBRID`，同时引用上述本地大纲和 5 条网页结果。
  - 主推荐为黑马程序员 Spring Boot 3 + Vue 3 课程；响应模型身份为
    `deepseek / deepseek-v4-pro`，无联网降级警告。
- 自动化验证：
  - Java Maven 全量 100 项通过。
  - Python pytest 全量 223 项通过；Ruff 和 `git diff --check` 通过。
- 已知限制：课程网页或视频地址可能随平台调整；推荐结果仍需保留来源，并用官方
  文档核实版本事实。用户后续上传的项目相关资料作为补充，不默认改变 SYLLABUS
  顺序。

---

## 七、阶段 9：交互式课程学习平台（6 步）

### 9.1 课程、课时与个人进度

- 状态：✅ 已完成
- 用户行为：课程中心展示九模块 Java + AI 路线；登录后的“继续学习”返回个人第一
  个未完成的已发布课时。
- 实现方法：Java/MySQL 保存课程、模块、课时、来源和 owner 隔离的学习进度；
  `practiceCompleted` 只由服务端判定，不能由浏览器伪造。
- 数据库迁移：V22。
- 提交：`feat: add structured course learning model`。

### 9.2 首节项目制示范课

- 状态：✅ 已完成
- 用户行为：可以学习“Controller、REST API 与参数校验”，同时查看黑马程序员
  `BV14z4y1N7pg` 第 15/16 分 P、Spring 官方文档和 StudyPilot 注册接口代码。
- 实现方法：版本化 JSON 课程目录在启动时幂等导入；只发布首节完整示范课，其余
  八个模块保留清晰路线和“即将开放”状态。
- 提交：`feat: publish first project based lesson`。

### 9.3 当前课时 AI 导师

- 状态：✅ 已完成
- 用户行为：在课时右侧连续追问 DTO、Controller、校验和项目代码，回答显示真实
  DeepSeek provider/model 并保持上下文。
- 实现方法：Java 提供 owner 校验的课时上下文与公共 Agent 门面；Python 保存加密
  会话，提示词限定当前课时、来源、进度和薄弱点，不声称观看或转录视频。
- 真实兼容修复：DeepSeek 当前不接受默认 `json_schema response_format`，改用
  `json_mode` 后再由 Pydantic 严格校验。
- 提交：`feat: teach within the active lesson context`。

### 9.4 课程中心、播放器与学习页

- 状态：✅ 已完成
- 用户行为：新增 `/courses`、`/courses/:slug`、`/lessons/:lessonId`；首页第一张
  主卡为继续学习。课时页包含 B 站播放器、回退链接、讲义、项目代码、来源和导师。
- 实现方法：播放器只接受合法 BV 号与分 P；iframe 使用 sandbox、CSP 和来源限制；
  Markdown 经 DOMPurify 消毒。视频由浏览器直接访问 B 站，不消耗 AI 搜索额度。
- 提交：`feat: make course learning the primary experience`。

### 9.5 检查题、课时测验与掌握度

- 状态：✅ 已完成
- 用户行为：先完成 Java 确定性检查题，再从课时生成固定 5 题；达到 60 分后完成
  课时并更新知识点掌握度。
- 实现方法：V23 将 quiz 与 lesson 关联；`taskId` 和 `lessonId` 恰好选择一个。选择题
  由 Java 判分，编程题继续由 DeepSeek 做不执行代码的文本评价。
- 数据库迁移：V23。
- 提交：`feat: close the lesson practice and mastery loop`。

### 9.6 真实端到端教学验收

- 状态：✅ 已完成
- 联调文件：`docs/course-learning-e2e.http`。
- 验收记录：`docs/course-learning-e2e-result.md`。
- 真实结果：
  - 课程返回 4 条可追溯来源，B 站第 15/16 分 P 与原页面回退均可定位。
  - 课内导师实际调用 `deepseek / deepseek-v4-pro`，成功返回课内解释。
  - DeepSeek 生成 5 题；attempt 为 `GRADED`、得分 100，产生 5 个掌握度记录。
  - 视频、讲义、检查题和测验完成后，课时进入 `COMPLETED`。
  - 当前没有下一节已发布课时，“继续学习”按契约返回 204。
- 联调修复：
  - `lessons.content_json` 显式映射 MySQL `LONGTEXT`，避免 Hibernate 启动校验失败。
  - DeepSeek 结构化输出使用兼容的 `json_mode`。
  - 全部课时完成时从错误的 404 修正为 204，并增加 Controller 回归测试。
- 自动化验证：
  - Java Maven 全量 112 项通过。
  - Python pytest 全量 233 项通过，Ruff 通过。
  - Vue Vitest 全量 56 项通过，vue-tsc 与生产构建通过。
  - `git diff --check` 通过。
- 提交：`test: complete interactive course learning workflow`。

### 阶段 9 当前边界

- 只有首节示范课完整发布；后续工作是按同一模板逐节制作其余八模块内容，而不是
  再重建课程基础设施。
- B 站站外播放器受平台、地区和 Cookie 策略影响；失败时跳转原页面。
- 不抓取视频、字幕，不保存 B 站 Cookie，不读取跨域播放秒数。
- 打开课程与已有资料不消耗 Tavily/DeepSeek；主动提问、搜索、生成测验和代码评价
  才会产生外部调用。

## 八、Roadmap Domain Foundation

- 状态：✅ 领域基础已完成，真实 MySQL 与联调结果以本节后续验收记录为准。
- 新建独立 Roadmap 领域，没有改名复用 Course/Lesson；旧课程和课时继续作为只读兼容
  历史存在。
- 发布 12 阶段、64 节点的 Java + AI v1 模板，并保存 79 条前置依赖关系。
- 用户可绑定当前路线，并查看完整地图、阶段、节点、依赖以及相互独立的学习、打卡、
  测验、成果与完成状态。
- 重复绑定同一模板版本返回同一个 enrollment；路线查询从当前登录身份取用户 ID，前端
  不提交 `ownerId`。
- 旧课程历史仅作为兼容证据，不授予新节点完成，也不会伪造打卡、测验通过或成果验收。
- 前端以 `/roadmap` 作为主要学习入口，同时保留旧 Course/Lesson 直达链接的兼容提示。
- 设计、实施和联调入口：
  - `docs/superpowers/specs/2026-08-09-roadmap-driven-self-learning-and-project-agent-design.md`
  - `docs/superpowers/plans/2026-08-09-roadmap-domain-foundation.md`
  - `docs/roadmap-foundation-e2e.http`
- 自动化验证（2026-08-09 新鲜执行）：Java Maven 全量 209 项通过；Vue Vitest 全量
  91 项通过；`vue-tsc --noEmit` 与 Vite 生产构建通过。
- 真实 MySQL 验证：迁移前已在仓库外生成一致性备份；Flyway 从 V23 成功迁移至
  V24。启动后目录为 1 个模板、12 个阶段、64 个节点、79 条依赖，旧 Course/Lesson
  数量迁移前后均为 1/9。
- 真实 HTTP 验证：使用一次性本地账号完成登录、绑定、地图、阶段、节点和重复绑定；
  地图返回 12/64，重复绑定返回相同 enrollment ID。
- 人工浏览器验收：登录用户显式绑定路线后，`/roadmap` 成功加载；图形视图 DOM 包含
  12 个阶段和 64 张节点卡片，切换列表视图后仍为 12 个阶段标题和 64 张节点卡片，
  两种视图的首节点标题一致。页面可见锁定节点的前置依赖说明，整体截图和视觉布局已
  检查。
- 导航与兼容验收：侧边栏显示 Roadmap，不再显示 Course 菜单项；直接访问 `/courses`、
  `/courses/studypilot-java-ai` 和 `/lessons/lesson-rest-controller` 均显示统一的旧路线状态
  提示，并可跳转 `/roadmap`。
- 网络边界验收：路线图功能请求通过 Java `:8080` 成功完成，前端 Roadmap 生产源码扫描
  未发现 FastAPI `/internal/**` 或 `:8000` 调用。浏览器控制接口没有暴露 Network 面板
  或性能条目，因此本次不声称完成了包级网络抓取。旧课时的 B 站嵌入产生一条不阻塞
  页面使用的第三方指纹控制台错误，不属于 Roadmap API 路径。
- 已知限制：本阶段只建立版本化路线目录、用户绑定、地图读取、升级预览和旧课程兼容；
  尚未实现入门诊断、滚动 7 日计划、节点打卡、成果验收和节点测验闭环。
- 下一步：实现入门诊断、滚动 7 日计划、打卡和成果闭环，再接入有引用的节点学习助手
  与受治理的项目操作 Agent。

### Roadmap V2 第一批：细粒度路线与三级导航

- 状态：✅ 已完成并通过规格、质量和全量回归。
- 路线目录从 V1 的 12 阶段、64 节点升级为并存的不可变 V2：12 阶段、24 模块、
  125 个 30～60 分钟节点；V1 历史与旧课程入口继续保留。
- Java 第一阶段已拆成 25 个从环境、变量、流程控制逐步进入面向对象、集合、异常、
  Stream、record/sealed 与工程规范的节点，不再把高级主题混入基本语法。
- 625 道节点题目均按主题手工编写并设置独立回归证据，重点覆盖高频开发、真实错误、
  可执行命令和安全防御；禁用批量模板、危险攻击导向与已知错误示例。
- V1→V2 升级先展示节点和模块差异。只有唯一显式映射且教学、考核、必修属性和先修
  契约完全等价的完成状态才会继承；宽泛节点不会一对多扩散，歧义映射必须人工处理。
- 已兼容历史升级预览：确认时在数据库锁内重算差异与映射，旧错误分类可安全规范化，
  真正歧义继续拒绝，重复确认返回相同结果且不丢失旧进度。
- 新增模块公共查询，浏览器调用链为 Stage → Module → Node；模块页展示进度、里程碑、
  有序节点、跨模块前置条件和锁定原因，V1 无模块数据时自动回退旧节点列表。
- 自动化验证（2026-08-14 新鲜执行）：Java Maven 246/246；Vue Vitest 100/100；
  `vue-tsc --noEmit`、Vite production build 和 `git diff --check` 均通过。
- 详细任务清单：
  `docs/superpowers/plans/2026-08-13-granular-roadmap-learning-loop-v2.md`。
- 下一步：第二批学习主闭环，包括节点总结打卡、持久化测验任务、自动生成 5 题、
  七日滚动计划、诊断快速验证与阶段毕业。

### 错题集：查看、重做与掌握闭环

- 状态：✅ 已完成。Java 279 项、Python 249 项、前端 115 项测试全部通过，
  TypeScript 检查、生产构建和 Ruff 均通过；真实 MySQL 已由 Flyway 从 V36 升级到 V37，
  Spring Boot 独立端口健康检查返回 `UP`，历史错题补录未阻塞启动。
- 测验进入终态后，Java 会把选择题错误和低于 70 分的编程题自动收录；同一道原题
  合并累计次数，后续答对一次转为“已掌握”，再次答错会重新激活。
- 错题详情同时展示题目、最近错误答案、正确答案或参考实现、解析、知识点和来源；
  V2 路线题按模块归类，旧课程按课程模块归类，其他测验按任务或测验标题兜底。
- “重做错题”支持全部错题和章节筛选，每组最多 5 题，末组不足 5 题仍可作答；
  题目直接复制原题，不重新调用 DeepSeek 或 Tavily。编程题重做仍使用原有异步 AI 评分。
- 重做继续复用 `/api/quizzes/{id}` 和 attempt 判分链路，作答前不返回正确答案，提交后
  返回完整答案对照；完成后显示本次清除数和全局剩余数。
- 应用启动后会按作答时间幂等回放历史终态测验，唯一事件约束保证重启不会重复计数。
- 详细实施清单：`docs/superpowers/plans/2026-09-03-wrong-question-book.md`。

### Roadmap V2 第三批 Task 11：工作区与实践成果登记

- 状态：✅ 已完成。该小步只登记成果证据，不执行 Maven、npm、pytest、Shell、Git
  写入或键鼠操作；安全 Runner 从 Task 12 开始独立建设。
- 新增登录用户专属的工作区登记与查询接口。工作区必须是已存在的绝对目录，保存前
  转换为真实规范路径；相对路径和符号链接根目录会被拒绝。
- 新增路线实践成果提交与查询接口。成果只接受工作区内的相对路径，并拒绝 `..`、
  符号链接和规范化后越界，防止目录穿越或软链接逃逸。
- 提交时冻结路线阶段、模块、节点标题和 ID，同时保存用户描述、测试证据、评价方式、
  提交版本及不可变状态事件。后续模板内容变化不会改写历史成果。
- 成果首次提交进入 `SUBMITTED`，只表示“已提交、待验收”，不会提前把里程碑标记为
  完成或 `ACCEPTED`；Task 14 才接入 AI Rubric 和用户最终确认。
- 所有查询从 Bearer Token 获取用户身份；其他用户无法查看工作区或成果。相同幂等键
  和相同内容返回原成果，不会新增版本或历史事件。
- 数据库使用新增的 Flyway V38 迁移，不修改任何已有历史迁移。
- 自动化验证（2026-09-04 新鲜执行）：Java Maven 284/284；覆盖工作区规范化与隔离、
  非法路径、成果快照、幂等、状态变化和跨用户访问。真实 MySQL 已由 Flyway 从 V37
  迁移至 V38，临时端口启动后的健康状态为 `UP`。详细任务清单见
  `docs/superpowers/plans/2026-08-13-granular-roadmap-learning-loop-v2.md`。
- 下一步：Task 12，增加固定 Maven Wrapper、npm、pytest 模板的执行预览与高风险依赖
  准备确认；本步骤不会直接扩张为任意命令执行器。

### Agent 原生化 Task 12：统一契约与能力矩阵

- 状态：✅ 已完成。StudyPilot 后续不再继续增加彼此割裂的 Agent 页面，而是建设统一
  会话入口，并保留原页面作为可靠备用入口。
- 新增机器可校验的工具契约，固定 `READ / NAVIGATE / WRITE / LOCAL` 四种效果、
  `NONE / LOW / HIGH` 三档风险，以及单轮 8 次工具、1 次联网、1 个写事务和 1 个高风险
  待确认动作的预算。
- 新增白名单 UI Action 与 SSE 事件契约。前端只能按 route key 导航或执行固定界面动作，
  不能执行模型提供的 URL、JavaScript、HTML 或 CSS selector；SSE 只公开过程摘要并支持
  按序号恢复，不展示模型内部思维链。
- 为当前路线、任务、测验、错题、资料、设置、审计和成果等页面建立能力矩阵，明确每页
  可以查询、导航和写入的能力。Agent 不得代答测验、伪造打卡、最终接受成果或扩大授权。
- 删除 AI 凭据、Runner 依赖准备、应用代码补丁、Git commit 和 push 固定为逐次确认，
  不能被普通聊天文本或长期授权绕过。
- 详细设计和任务清单见 `docs/superpowers/specs/2026-09-04-agent-native-studypilot-design.md`
  与 `docs/superpowers/plans/2026-09-04-agent-native-studypilot.md`。
- 下一步：Task 13，实现 Java 类型化工具目录、动态学习上下文和第一批只读/导航工具。

### Agent 原生化 Task 13：Java 工具目录与动态学习上下文

- 状态：✅ 已完成。新增内部工具目录 `GET /internal/agent-tools/catalog` 和统一调用入口
  `POST /internal/agent-tools/{toolName}/invoke`，两者都必须携带内部服务令牌，浏览器无法
  直接访问。
- Java 使用显式注册的类型化 Handler，不接受模型提供的 URL、SQL、Bean、类名或 Shell。
  工具名重复会阻止服务启动，未知工具、未声明参数、类型错误和模型伪造的 `ownerId`
  都会在进入业务 Service 前被拒绝。
- 工具参数限制为 32 KiB，返回限制为 64 KiB；超限输出会替换为安全裁剪提示，避免将
  整份资料、全部历史或异常大对象塞回模型上下文。
- 第一批只读工具已经覆盖学习上下文、目标、计划、任务、路线、阶段、模块、节点、
  测验、掌握度、错题概况、资料、通知、治理记录、审计、工作区和 AI 配置状态。
  所有工具都把内部请求中的用户身份作为独立上下文传给原有 Service，继续复用既有
  用户隔离规则。
- 动态学习快照会在每次调用时重新聚合学习事实、当前路线、错题数、未读通知、待确认
  执行和工作区；用户尚未绑定路线时返回可恢复警告，不让整轮对话失败。
- 导航解析只接受 Route Registry 中的 route key 和必要实体 ID，拒绝任意 URL、脚本值、
  多余参数和不完整参数。当前只生成目标，不直接模拟点击。
- 下一步：Task 14，把生成测验、错题重做、任务和计划调整等写工具接入统一风险治理，
  并确保普通聊天文字永远不能充当执行确认。

### Agent 原生化 Task 14：业务写工具与分级治理

- 状态：✅ 已完成。统一工具网关现已区分只读/导航与写工具；所有写工具必须提供稳定
  幂等键，并先创建持久化 `AgentToolAction` 和原有 `AgentExecution`，模型输出不会直接
  修改业务数据。
- 第一批受治理写工具覆盖学习任务状态、错题重做批次、路线节点重测，以及文本/网页
  资料导入。每个 Handler 都显式声明版本、风险、授权范围、执行类型、输入 Schema 和
  不包含敏感正文的操作摘要。
- 任务完成、跳过和延期固定为 HIGH 风险，即使存在长期授权也进入
  `WAITING_CONFIRMATION`；未调用专用确认接口前，任务状态、版本和历史均保持不变。
  普通消息参数中夹带 `confirm` 会因未声明字段被拒绝。
- LOW 风险操作在长期授权覆盖时可以执行，否则进入 `WAITING_AUTHORIZATION`。等待动作
  生成通知；执行成功和失败也会生成通知，并沿用 AgentExecution 与审计日志记录状态。
- 新增 15 分钟确认有效期、用户隔离、数据库幂等唯一约束和悲观锁。重复确认成功动作
  直接返回原结果，不会重复修改业务版本或新增历史；其他用户无法确认该动作。
- 业务 Handler 在独立事务中执行。业务失败会回滚自身修改，外层仍能保存 `FAILED`
  操作、失败执行记录和通知，避免出现“实际失败但审计仍停在 RUNNING”的状态。
- Flyway V39 新增 `agent_tool_actions`，不修改任何历史迁移。
- 下一步：Task 15，在 Python 中实现统一 LangGraph Supervisor，复用现有计划、任务、
  知识问答和教学能力，并通过新工具目录调用 Java。

### Agent 原生化 Task 15：Unified LangGraph Supervisor

- 状态：✅ 已完成。FastAPI 新增统一会话内部入口，作为下一步 Java 公共门面和 SSE 的
  编排核心；原有计划、任务、知识问答和课内导师服务继续存在，后续按意图委托，不做
  一次性推倒重写。
- 每轮对话首先调用 Java `learning.context.get` 获取最新路线、学习事实、错题、通知、
  待确认动作和工作区上下文。前端传来的 route 信息只作界面提示，用户身份和实体归属
  始终由 Java 重新校验。
- 已打通两个关键场景：“继续昨天没学完的章节”从 Java 的结构化路线状态找到进行中或
  下一个可学节点，再返回白名单 `ROADMAP_NODE` 动作；“打开错题集并重做五题”通过
  受治理写工具生成操作预览，不会直接创建副作用。
- 自然语言中的“确认”不能恢复待执行动作；只有 Task 16 提供的 Java 专用确认门面才能
  确认。相同消息幂等键直接返回原轮次结果，并发消息返回稳定冲突。
- 新增统一工具预算：每轮最多 8 次工具、1 次联网搜索、1 次写操作、1 个高风险待确认；
  相同工具和参数禁止重复调用。工具名称必须存在于 Java 类型化目录，Python 不接受模型
  提供的 URL、SQL、Bean 或任意执行目标。
- Supervisor 使用 LangGraph 编排确定性高置信度路由；低置信度写意图先要求澄清，不猜测
  写操作。Java 返回的资料标题、路线正文和网页证据均只按结构化数据读取，不能成为工具
  指令。
- 新增内部接口：创建统一会话、发送消息和查询快照；均要求内部服务令牌，只预留给
  Task 16 的 Java Agent Facade，浏览器仍不能直连 FastAPI。
- 自动化验证（2026-09-04 新鲜执行）：Python 全量 260/260，Ruff 和
  `git diff --check` 通过。
- 已知限制：本小步会话仍是进程内存，尚未提供公开 Bearer API、SSE、取消和刷新恢复；
  它们属于紧接着的 Task 16。更多页面意图与旧专用子图的完整委托在 Task 18 补齐。
- 下一步：Task 16，把统一会话、轮次和可重放事件写入加密 SQLite，并由 Java 提供认证
  公共门面与 SSE 断线续传。

### Agent 原生化 Task 16：持久化统一会话与 SSE 门面

- 状态：✅ 已完成。统一 Agent 不再只保存于 FastAPI 进程内存；会话快照、消息历史、
  幂等轮次结果和事件流均复用现有 AES-GCM 加密 SQLite，磁盘中不保存用户 ID、消息或
  回答明文。
- 服务重启或运行时缓存丢失后，会按会话 ID 与 owner 哈希懒加载状态。相同幂等键即使
  跨重启再次提交，也返回原轮次结果，不重复调用 Java 工具或产生副作用。
- 每个会话维护严格递增事件序号，公开 `TURN_STARTED`、`CONTEXT_LOADED`、工具成功、
  操作预览、UI Action、完成和失败摘要，不输出模型内部思维链。工具异常会记录
  `TURN_FAILED` 并释放活动轮次，避免会话永久卡在忙碌状态。
- FastAPI 新增事件游标、专用动作确认/拒绝和轮次取消内部接口。普通消息仍不能代替
  确认；取消采用协作式停止，保证已经落库的确定性操作不会被伪装成“已撤销”。
- Java 新增 `/api/assistant/conversations/**` 公共门面，覆盖创建、发送消息、查询、事件、
  确认、拒绝和取消。浏览器只携带 Bearer Token，Java 始终覆盖伪造的 `ownerId` 并保管
  内部服务令牌。
- SSE 门面返回标准 `text/event-stream`，包含心跳、`id`、`event` 和 JSON `data`。
  浏览器重连时提交 `Last-Event-ID`，Java 转为 Python 的 `afterSequence`，只回放后续事件。
  事件类型和序号会再次校验，不能注入换行或伪造 SSE 字段。
- 新的统一消息入口同时纳入原 Agent 用户级和模型级限流。
- 自动化验证（2026-09-04 新鲜执行）：Java Maven 302/302，Python pytest 269/269，
  Ruff 与 `git diff --check` 通过。
- 已知限制：当前消息 POST 仍等待编排完成后返回，SSE 主要提供可恢复的过程回放；真正
  token 级 `ASSISTANT_DELTA` 会在前端 Task 17 接入流式页面时继续优化。FastAPI 仍按设计
  限制单 worker。
- 下一步：Task 17，把 `/assistant` 建成登录默认首页，接入事件过程卡片、操作确认卡片和
  白名单 UI Action Dispatcher。

### Agent 原生化 Task 17：Agent 首页与前端动作执行器

- 状态：✅ 已完成。登录后的根路径现在是统一 Agent 首页，原工作台移动到
  `/dashboard`；侧边栏顶部固定提供“Agent 首页”，路线、今日任务、资料、错题和传统
  Agent 页面仍全部保留，可在 AI 降级时手动操作。
- 首页沿用现有色彩、圆角、间距和响应式规则，新增四个高频快捷指令、统一对话区、模型
  状态、公开工具过程卡片和风险操作预览。助手回答复用安全 Markdown 组件，用户消息仍
  以纯文本显示。
- 页面只调用 Java `/api/assistant/**`，不会直连 FastAPI 或读取内部令牌。会话 ID 只在
  `sessionStorage` 中保存，刷新后会从加密后端状态恢复；会话失效时安全创建新会话。
- UI Action 使用前端独立 Route Registry 做第二层校验。目前只自动执行白名单导航，
  route key、参数数量、参数名称和实体 ID 格式必须完全匹配；任意 URL、JavaScript、
  HTML、CSS selector 和多余参数都会被拒绝。
- 写操作预览显示摘要与风险等级，只能通过卡片按钮调用专用确认或拒绝 API。自动导航
  失败不会丢失对话，并提示用户使用保留的传统菜单。
- 本阶段使用 `frontend-design` 技能保持既有设计体系，同时把 Agent 页面做成清晰的主
  工作区，而不是再增加一个普通表单页面。
- 自动化验证（2026-09-04 新鲜执行）：Vue Vitest 119/119、`vue-tsc --noEmit`、Vite
  production build 与 `git diff --check` 通过。
- 下一步：Task 18，逐项补齐所有 StudyPilot 页面对应的查询、导航和受治理写工具，并
  将知识问答、计划、任务与教学子图真正委托到统一会话。

### Agent 原生化 Task 18：通过对话操作全部 StudyPilot 业务

- 状态：✅ 已完成。Java Tool Registry 现在覆盖路线、阶段、模块、节点、今日安排、学习
  目标与计划、测验、错题、掌握度、资料、通知、执行审计、学习设置、AI 配置状态、工作区
  和成果。统一 Agent 在导航前先查询真实业务数据，不依靠模型猜测页面状态。
- 新增路线加入/升级、目标和计划创建、日程刷新、节点测验生成、错题重做、通知已读、
  学习设置、工作区登记与成果提交等受治理写工具。写操作继续使用类型化参数、幂等键、
  AgentExecution、授权、通知和审计；路线、设置、工作区和成果写入固定为高风险确认。
- “开始当前节点测验”“查看今日安排”“总结薄弱点”“检查 AI 配置”等常用指令能够查询
  Java 事实后返回白名单页面动作。前端新增工作区与成果页面，并补齐目标、计划、传统
  Agent 页面和工作区的 Route Registry 映射。
- “查找/解释/怎么学”等学习请求委托阶段 6 的知识问答服务，继续使用本地资料、Tavily
  与 DeepSeek，并在统一对话中展示引用及降级警告。同一统一会话复用知识子会话，因此
  “解释一下它”之类的连续追问保留上下文；子会话 ID 随加密状态持久化。
- 能力矩阵已与真实工具名对齐。答题、打卡总结、自评、AI Key 输入和成果最终接受没有
  暴露给模型，仍必须由用户在专用界面完成，防止 Agent 代替学习或扩大权限。
- 新增工具覆盖、意图路由、知识上下文、恶意引用链接和页面导航测试。完整验证结果记录在
  本 Task 的提交中。自动化验证（2026-09-04 新鲜执行）：Java Maven 304/304、Python
  pytest 274/274、Vue Vitest 121/121，Ruff、TypeScript、生产构建与 `git diff --check`
  全部通过。
- 下一步：Task 19，增加用户可管理的主动自动化规则、持久化任务租约、授权范围内的低风险
  自动执行，以及高风险事项只通知不执行的安全边界。

---

## 九、每完成一个小功能如何更新本文档

以后每完成一个小功能，都执行以下记录流程：

1. 将对应状态从 `⬜ 未开始` 改为 `🚧 开发中`，完成后改为 `✅ 已完成`。
2. 记录用户可以看到的行为，而不只写技术名词。
3. 记录主要开发方法、关键文件和为什么采用这种设计。
4. 记录自动化测试数量、手动联调结果和已知限制。
5. 记录对应 Git 提交，方便从文档定位到代码版本。

每个小功能仍遵守统一开发周期：

```text
确定行为与验收条件
→ 写失败测试
→ 实现最小代码
→ 运行局部测试
→ 运行全量测试
→ 更新本开发步骤文档
→ 提交并推送 GitHub
```
