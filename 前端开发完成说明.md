# StudyPilot 前端开发完成说明

> 面向对象：负责审查与联调的工程师或 AI 编程助手
>
> 完成日期：2026-07-28
>
> 开发基线：`docs/前端开发对接说明.md`（阶段 7 完成版本，迁移 V1～V20）
>
> 代码位置：`web/`（Vue 3 + TypeScript + Vite + Pinia + Vue Router + Axios）

本文是前端交付的交接上下文，与 `web/README.md` 互补：README 偏使用说明，
本文偏**契约落实、设计决策与审查要点**。

---

## 1. 交付状态

| 验证项 | 结果 |
|---|---|
| `npm run typecheck`（vue-tsc --noEmit） | ✅ 通过，0 错误 |
| `npm run test`（vitest） | ✅ 18 个用例全部通过（4 个文件） |
| `npm run build`（typecheck + vite build） | ✅ 通过 |
| dev server 冒烟（`/`、`/login` 200，模块转换正常） | ✅ 通过 |
| 全代码扫描 `/internal/**`、内部令牌、Python 地址、模型 Key | ✅ 零命中 |
| 未修改 `backend/`、`ai-service/`、`docs/` | ✅ 仅 `web/` 新增/修改 |

## 2. 启动方式

```bash
cd web
npm install
npm run dev        # http://localhost:5173
```

环境变量（`.env.example` / `.env.development`，仅两个，均非敏感）：

```bash
VITE_API_BASE_URL=http://localhost:8080   # Java 公共地址，浏览器唯一出口
VITE_AGENT_GATEWAY=mock                   # mock（默认）/ http
```

Node ≥ 20.19（开发验证环境：Node v26.5 / npm 11.17）。

## 3. 页面完成情况

### 3.1 ✅ 真实接口页面（对接当前 Java `/api/**`）

| 路由 | 页面 | 落实的契约要点 |
|---|---|---|
| `/login` | 登录 | 无需 Token；`?expired=1` 展示 401 过期提示；支持 `?redirect=` 回跳 |
| `/register` | 注册 | 前端校验（邮箱格式、密码 8～72、昵称 ≤80、两次一致）；400 优先展示 `fieldErrors` |
| `/` | 工作台 | `GET /api/dashboard` 六项统计 + 全空时的新手引导 |
| `/goals` | 目标 | 创建/编辑；前端预校验目标日期未来、1～40 小时 |
| `/plans` | 计划列表 | 创建（日期先后校验）、DRAFT 计划确认按钮，409 后刷新 |
| `/plans/:id` | 计划详情 | 确认计划（ConfirmDialog）；仅 CONFIRMED 可加任务（5～720 分钟）；版本历史展开查看 `snapshotJson`（pretty-print）；任务按日期排序 |
| `/today` | 今日任务 | `?date=` 查询任意日期；完成（实际用时 1～720）/跳过（原因）/延期（必须新日期）三个专用弹窗；变更历史展开；进度条汇总 |
| `/materials` | 资料 | 三种导入（文本 ≤200,000 字符 / 网页 URL 校验 / 文件 ≤20MB）；隐私级别说明；SENSITIVE/LOCAL_ONLY 展示不上云提示 |
| `/materials/:id` | 资料详情 | `PENDING/PROCESSING` 每 3s 轮询，`READY/FAILED` 停止；失败原因、摘要、标签、知识点、warnings 完整展示 |
| `/quizzes/:id` | 测验作答 | 单选/多选/编程题三种题型；starterCode 预填；幂等键 `quiz-attempt:{quizId}:{crypto.randomUUID()}`，同一轮重试复用；未答题目确认时提示 |
| `/attempts/:id` | 测验结果 | `EVALUATING` 每 2s 轮询；`warning` 显著展示；逐题结果 + AI 评价详情折叠；按知识点自评（0～100） |
| `/mastery` | 掌握度 | 综合分 + 测验/任务/自评三分量；**缺失分量显式显示 `—`（非 0）**；证据/作答次数 |
| `/notifications` | 通知 | 单条/全部标已读，以服务端返回为准更新 |
| `/activity` | 执行与审计 | 三个 Tab：执行记录（`WAITING_CONFIRMATION` 角标 + 专用确认弹窗，展示 summary/风险/影响范围/幂等键/费用）、授权创建（scopes 多选 + 有效期）、审计表 |
| `/settings` | 学习设置 | 时区、每日上限 15～720、周末偏好、默认隐私级别、每周可用时段（结束>开始校验；`HH:mm` 归一化为 `HH:mm:ss`） |

### 3.2 🟨 Mock 页面（经 AgentGateway 接口，等阶段 8 Java 门面）

| 路由 | 页面 | 状态机落实 |
|---|---|---|
| `/agent/plan` | 对话生成计划 | `COLLECTING → DRAFT_READY → (专用 confirm) → COMPLETED`；草案以结构化表单展示；修改意见转自然语言消息换取**完整新草稿**；支持取消页面等待（不取消服务端任务）；`?goalId=` 预选 |
| `/agent/tasks` | 任务 Agent | `COLLECTING → PREVIEW_READY → (专用 confirm) → EXECUTING → COMPLETED`；预览展示候选任务、动作、原因、延期日期、实际用时、版本影响；确认中禁按钮；409 提示刷新 |
| `/knowledge` | 知识问答 | `AUTO / LOCAL_ONLY` 模式 + `AUTO/ENABLED/DISABLED` 联网策略；引用为一等 UI（来源类型标签、定位、可折叠片段、`rel="noopener noreferrer"` 外链）；无引用时明确标记；Tavily 未配置降级警告；`resultId` 网页来源可确认导入 |
| `/plans/:id` 内嵌 | 计划自适应调整 | `ANALYZING → DRAFT_READY/NO_CHANGE → (专用 confirm) → EXECUTING → COMPLETED`；偏差信号、四种操作类型、LOW/HIGH 风险（HIGH 红色确认按钮）、前后版本号展示 |
| 任务行按钮 | 从任务生成测验 | loading 防重复点击；Mock 模式提示不落库，http 模式自动跳 `/quizzes/{quizId}` |
| `/settings/ai` | AI 设置 | Key 仅存组件临时内存，提交成功即清空；只展示 `configured` + 脱敏尾号；删除操作 |

所有 Mock 页面顶部有醒目 Mock 横幅，侧栏导航带 `Mock` 角标，用户不会误以为数据已落库。

## 4. 架构与关键设计决策

### 4.1 服务层分层（页面不直接碰 Axios）

```text
页面/组件
  └── services/current/*     ✅ 已公开 Java API 的强类型封装（页面唯一入口）
  └── services/planned/*     🟨 AgentGateway 接口
        ├── mock.ts          MockAgentGateway（内存状态机，默认）
        ├── httpGateway.ts   HttpAgentGateway（/api/agent/**，待门面）
        └── index.ts         按 VITE_AGENT_GATEWAY 选择实现
  └── services/http.ts       Axios 实例 + 拦截器
```

### 4.2 认证与 401

- Token 存 `sessionStorage`（键 `studypilot.accessToken`），不用 LocalStorage。
- 请求拦截器统一注入 `Authorization: Bearer`；注册/登录接口本身不需要 Token。
- 401 由响应拦截器统一处理：清理 sessionStorage → 调用 `main.ts` 注册的
  回调 → auth store 清空 → 路由跳 `/login?expired=1`。页面零重复处理。
- 应用恢复时路由守卫先调 `/api/auth/me` 验证，不信任本地缓存（有测试覆盖）。

### 4.3 确认边界

- 统一 `ConfirmDialog` 组件，确认按钮文案强制带对象和动作
  （如「确认完成「阅读第 3 章」」「确认执行 3 项计划调整」），无裸「确定」。
- 高风险操作（HIGH 风险执行/调整）使用红色危险按钮。
- 聊天输入中的“确认/可以”只作为消息发送，无任何写副作用——Mock 实现同样
  遵守，避免页面形成错误交互习惯。

### 4.4 异步轮询（`composables/usePolling.ts`）

| 对象 | 间隔 | 终止状态 |
|---|---|---|
| 资料解析 | 3s | `READY` / `FAILED` |
| 测验评分 | 2s | `GRADED` / `PARTIALLY_GRADED` |
| 调整分析/执行 | 2s | 非 `ANALYZING`/`EXECUTING` |

特性：连续错误指数退避（×2^n），5 次后停止并展示「恢复轮询」按钮；
`document.hidden` 时降频 3 倍，重新可见时 200ms 内补一次；组件卸载自动清理
（有测试覆盖三种行为）。

### 4.5 幂等与 409

- 测验提交：幂等键在每次点击提交时生成一次并缓存，网络失败重试复用原键；
  新一轮作答提交才生成新键。
- 所有 409（任务状态、计划确认、执行确认、调整确认）：不自动重试写操作，
  toast 提示 + 重新拉取服务端最新数据。

### 4.6 展示层约定

- 服务端枚举全程保留英文原值，中文仅在 `utils/labels.ts` 映射。
- 日期 `YYYY-MM-DD`、时间 `HH:mm:ss`、时间戳 ISO 8601；展示用 `formatDateTime`。
- 可空数值（actualMinutes、score、分量、estimatedCost 等）显式判 `null`，
  缺失显示 `—`，不用真假判断吞掉 0。

## 5. 与说明书的偏差声明（审查重点）

1. **`AgentGateway` 接口扩展**（`types/agent.ts`）：说明书 §7.7 未列出但页面必需，
   已补充——
   - `getPlanAdjustment(id)` / `confirmPlanAdjustment(id)`：对应 §7.4 已给出的
     `GET/POST /api/agent/plan-adjustments/{id}(/confirm)` 端点（轮询与确认必需）；
   - `importWebResult(resultId, category, privacyLevel)`：对应 §5.4 已公开的
     真实接口 `POST /api/web-search-results/{resultId}/import`（走 http 实例）；
   - `getAiSettings` / DeepSeek/Tavily Key 的 PUT/DELETE：对应 §7.6。
2. **Mock 模式下的测验生成**：Mock 返回的 `quizId` 在 Java 不存在，因此 Mock 模式
   只 toast 提示不跳转（跳转会 404）；`VITE_AGENT_GATEWAY=http` 时恢复自动跳转。
   页面通过 `gatewayMode` 常量区分，未污染接口。
3. **任务列表**：`GET /api/learning-tasks` 无 `planId` 过滤参数，计划详情页采用
   拉全量后前端按 `planId` 过滤（数据量大时建议 Java 后续加查询参数）。
4. **计划详情数据**：无 `GET /api/learning-plans/{id}` 单查接口，从列表中匹配；
   404 场景（不存在或不属于当前用户）展示错误态而非崩溃。

除以上 4 点外，请求路径、请求体、字段名、枚举值与说明书契约完全一致。

## 6. 测试覆盖（`web/tests/`，18 用例）

| 文件 | 覆盖点 |
|---|---|
| `auth.spec.ts` | 登录存 sessionStorage、restore 走 `/me` 验证、restore 失败清理、退出强制清理 |
| `http.spec.ts` | 请求注入 Bearer、无 Token 不注入、401 清 Token + 回调、`describeError` 优先 fieldErrors、409 文案 |
| `polling.spec.ts` | 终止状态停止、错误退避上限触发 onFailed、卸载后不再请求 |
| `components.spec.ts` | ConfirmDialog 确认文案/loading 禁用/隐藏不渲染；注册页邮箱/密码/确认密码校验 |

## 7. 待联调清单（阶段 8 Java 门面就绪后）

1. `POST /api/agent/plan-conversations`、`POST .../{id}/messages`、`GET .../{id}`、`POST .../{id}/confirm`
2. `POST /api/agent/task-conversations`、`POST .../{id}/messages`、`GET .../{id}`、`POST .../{id}/confirm`
3. `POST /api/agent/knowledge-conversations`、`POST .../{id}/messages`、`GET .../{id}`
4. `POST /api/agent/plan-adjustments/analyze`、`GET .../{id}`、`POST .../{id}/confirm`
5. `POST /api/agent/quizzes/generate`（验证真实 quizId 跳转链路）
6. `GET /api/ai-settings`、`PUT/DELETE .../deepseek-key`、`PUT/DELETE .../tavily-key`

切换方式：`.env.development` 设 `VITE_AGENT_GATEWAY=http`，页面零改动。

联调建议关注点：会话类接口的**全量返回契约**（每次消息返回完整会话对象，
前端按此设计）；确认接口的 409 语义；`ANALYZING` 轮询的预期时长。

## 8. 文件清单

```text
web/
├── index.html  package.json  vite.config.ts  tsconfig.json
├── .env.example  .env.development  .gitignore  README.md
├── src/
│   ├── app/              main.ts / router.ts / App.vue
│   ├── components/       AppShell, ConfirmDialog, CitationCard, TaskList,
│   │                     StatusBadge, EmptyState, ErrorState, LoadingBlock,
│   │                     MockBanner, ToastHost
│   ├── composables/      usePolling.ts
│   ├── modules/          auth(2) dashboard(1) learning(4) materials(2)
│   │                     assessment(3) agent(5) settings(2) notifications(1)
│   │                     NotFoundView
│   ├── services/         http.ts + current/(6) + planned/(3)
│   ├── stores/           auth.ts toast.ts
│   ├── styles/           main.css（设计变量 + 通用类）
│   ├── types/            api.ts（当前契约）agent.ts（阶段 8 契约）
│   └── utils/            labels.ts（枚举→中文）datetime.ts
└── tests/                auth / http / polling / components（18 用例）
```
